## 基础运行验收

- [x] 项目可以正常启动开发服务器。
- [x] 浏览器打开登录页不白屏。
- [x] 控制台没有影响继续开发的明显错误。
- [x] `npm run build` 可以通过，允许存在体积提示。

## Git 版本管理验收

验收重点：必须使用 Git 管理项目开发过程，不得只保留最终代码而没有可追溯的提交记录。

- [x] 项目已使用 Git 进行版本管理，`git status` 可以正常查看工作区状态。
- [x] 开发前会使用 `git status`、`git log --oneline` 确认当前状态和提交基线。
- [x] 任务1、任务2、任务3的实现过程已拆分为职责清晰的提交，不将所有修改堆在一个提交中。
- [x] 提交信息能说明修改目的，第02天的提交 scope 统一使用 `day-2`，例如 `feat(day-2): implement authentication flow`。
- [x] 每次提交前已检查变更内容，没有误提交 `node_modules`、`dist`、本地环境文件或其他无关文件。
- [x] 通过 `git log --oneline` 可以看出第02天从认证接口、请求与路由守卫，到动态权限的实现过程。

## 作业提交格式验收

提交物最外层必须是一个独立文件夹，文件夹名统一使用“学号-姓名-day2”格式。


2024057226-仲琪娜-day2