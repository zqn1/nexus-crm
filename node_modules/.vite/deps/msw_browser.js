import {
  BatchInterceptor,
  DeferredPromise,
  Disposable,
  Emitter,
  HandlersController,
  HttpResponse,
  InMemoryHandlersController,
  Interceptor,
  InternalError,
  RequestHandler,
  TypedEvent,
  createRequestId,
  devUtils,
  executeHandlers,
  invariant,
  isCommonAssetRequest,
  isObject,
  kAutoConnect,
  kConnect,
  resolveWebSocketUrl,
  storeResponseCookies,
  toPublicUrl,
  toReadonlyArray,
  until
} from "./chunk-SZMHN3HQ.js";
import {
  __privateAdd,
  __privateGet,
  __privateMethod,
  __privateSet,
  __publicField
} from "./chunk-DWA4UIM3.js";

// node_modules/msw/lib/core/experimental/frames/network-frame.mjs
var NetworkFrame = class {
  constructor(protocol, data) {
    __publicField(this, "events");
    this.protocol = protocol;
    this.data = data;
    this.events = new Emitter();
  }
};

// node_modules/msw/lib/core/experimental/sources/network-source.mjs
var NetworkFrameEvent = class extends TypedEvent {
  constructor(type, frame) {
    super(...[type, {}]);
    __publicField(this, "frame");
    this.frame = frame;
  }
};
var NetworkSource = class {
  constructor() {
    __publicField(this, "emitter");
    this.emitter = new Emitter();
  }
  async queue(frame) {
    await this.emitter.emitAsPromise(
      // @ts-expect-error Trouble handling a conditional type parameter.
      new NetworkFrameEvent("frame", frame)
    );
  }
  on(type, listener, options) {
    this.emitter.on(type, listener, options);
  }
  disable() {
    this.emitter.removeAllListeners();
  }
};

// node_modules/msw/lib/core/experimental/request-utils.mjs
var REQUEST_INTENTION_HEADER_NAME = "x-msw-intention";
var RequestIntention = ((RequestIntention2) => {
  RequestIntention2["passthrough"] = "passthrough";
  return RequestIntention2;
})(RequestIntention || {});
function shouldBypassRequest(request) {
  var _a20;
  return !!((_a20 = request.headers.get("accept")) == null ? void 0 : _a20.includes("msw/passthrough"));
}
function isPassthroughResponse(response) {
  return response.status === 302 && response.headers.get(REQUEST_INTENTION_HEADER_NAME) === "passthrough";
}
function deleteRequestPassthroughHeader(request) {
  const acceptHeader = request.headers.get("accept");
  if (acceptHeader) {
    const nextAcceptHeader = acceptHeader.replace(/(,\s+)?msw\/passthrough/, "");
    if (nextAcceptHeader) {
      request.headers.set("accept", nextAcceptHeader);
    } else {
      request.headers.delete("accept");
    }
  }
}

// node_modules/msw/lib/core/experimental/frames/http-frame.mjs
var RequestEvent = class extends TypedEvent {
  constructor(type, data) {
    super(...[type, {}]);
    __publicField(this, "requestId");
    __publicField(this, "request");
    this.requestId = data.requestId;
    this.request = data.request;
  }
};
var ResponseEvent = class extends TypedEvent {
  constructor(type, data) {
    super(...[type, {}]);
    __publicField(this, "requestId");
    __publicField(this, "request");
    __publicField(this, "response");
    this.requestId = data.requestId;
    this.request = data.request;
    this.response = data.response;
  }
};
var UnhandledExceptionEvent = class extends TypedEvent {
  constructor(type, data) {
    super(...[type, {}]);
    __publicField(this, "error");
    __publicField(this, "requestId");
    __publicField(this, "request");
    this.error = data.error;
    this.requestId = data.requestId;
    this.request = data.request;
  }
};
var HttpNetworkFrame = class extends NetworkFrame {
  constructor(options) {
    const id = options.id || createRequestId();
    super("http", { id, request: options.request });
  }
  getHandlers(controller) {
    return controller.getHandlersByKind("request");
  }
  async getUnhandledMessage() {
    const { request } = this.data;
    const url = new URL(request.url);
    const publicUrl = toPublicUrl(url) + url.search;
    const requestBody = request.body == null ? null : await request.clone().text();
    const details = `

  • ${request.method} ${publicUrl}

${requestBody ? `  • Request body: ${requestBody}

` : ""}`;
    const message = `intercepted a request without a matching request handler:${details}If you still wish to intercept this unhandled request, please create a request handler for it.
Read more: https://mswjs.io/docs/http/intercepting-requests`;
    return message;
  }
  async resolve(handlers, onUnhandledFrame, resolutionContext) {
    const { id: requestId, request } = this.data;
    const requestCloneForLogs = (resolutionContext == null ? void 0 : resolutionContext.quiet) ? null : request.clone();
    this.events.emit(new RequestEvent("request:start", { requestId, request }));
    if (shouldBypassRequest(request)) {
      this.events.emit(new RequestEvent("request:end", { requestId, request }));
      this.passthrough();
      return null;
    }
    const [lookupError, lookupResult] = await until(() => {
      var _a20;
      return executeHandlers({
        requestId,
        request,
        handlers,
        resolutionContext: {
          baseUrl: (_a20 = resolutionContext == null ? void 0 : resolutionContext.baseUrl) == null ? void 0 : _a20.toString(),
          quiet: resolutionContext == null ? void 0 : resolutionContext.quiet
        }
      });
    });
    if (lookupError != null) {
      if (!this.events.emit(
        new UnhandledExceptionEvent("unhandledException", {
          error: lookupError,
          requestId,
          request
        })
      )) {
        console.error(lookupError);
        devUtils.error(
          'Encountered an unhandled exception during the handler lookup for "%s %s". Please see the original error above.',
          request.method,
          request.url
        );
      }
      this.errorWith(lookupError);
      return null;
    }
    if (lookupResult == null) {
      this.events.emit(
        new RequestEvent("request:unhandled", {
          requestId,
          request
        })
      );
      await executeUnhandledFrameHandle(this, onUnhandledFrame).then(
        () => this.passthrough(),
        (error2) => this.errorWith(error2)
      );
      this.events.emit(
        new RequestEvent("request:end", {
          requestId,
          request
        })
      );
      return false;
    }
    const { response, handler, parsedResult } = lookupResult;
    this.events.emit(
      new RequestEvent("request:match", {
        requestId,
        request
      })
    );
    if (response == null) {
      this.events.emit(
        new RequestEvent("request:end", {
          requestId,
          request
        })
      );
      this.passthrough();
      return null;
    }
    if (isPassthroughResponse(response)) {
      this.events.emit(
        new RequestEvent("request:end", {
          requestId,
          request
        })
      );
      this.passthrough();
      return null;
    }
    const responseCloneForLogs = (resolutionContext == null ? void 0 : resolutionContext.quiet) ? null : response.clone();
    await storeResponseCookies(request, response);
    this.respondWith(response);
    this.events.emit(
      new RequestEvent("request:end", {
        requestId,
        request
      })
    );
    if (!(resolutionContext == null ? void 0 : resolutionContext.quiet)) {
      handler.log({
        request: requestCloneForLogs,
        response: responseCloneForLogs,
        parsedResult
      });
    }
    return true;
  }
};

// node_modules/msw/lib/core/experimental/on-unhandled-frame.mjs
async function executeUnhandledFrameHandle(frame, handle) {
  const printStrategyMessage = async (strategy) => {
    if (strategy === "bypass") {
      return;
    }
    const message = await frame.getUnhandledMessage();
    switch (strategy) {
      case "warn": {
        return devUtils.warn("Warning: %s", message);
      }
      case "error": {
        return devUtils.error("Error: %s", message);
      }
    }
  };
  const applyStrategy = async (strategy) => {
    invariant.as(
      InternalError,
      strategy === "bypass" || strategy === "warn" || strategy === "error",
      /**
       * @fixme Rename "onUnhandledRequest" to "onUnhandledFrame" in the error message
       * with the next major release.
       */
      devUtils.formatMessage(
        'Failed to react to an unhandled network frame: unknown strategy "%s". Please provide one of the supported strategies ("bypass", "warn", "error") or a custom callback function as the value of the "onUnhandledRequest" option.',
        strategy
      )
    );
    if (strategy === "bypass") {
      return;
    }
    await printStrategyMessage(strategy);
    if (strategy === "error") {
      return Promise.reject(
        new InternalError(
          devUtils.formatMessage(
            'Cannot bypass a request when using the "error" strategy for the "onUnhandledRequest" option.'
          )
        )
      );
    }
  };
  if (typeof handle === "function") {
    return handle({
      frame,
      defaults: {
        warn: printStrategyMessage.bind(null, "warn"),
        /**
         * @note The defaults only print the corresponding messages now.
         * They do not affect the frame resolution (e.g. do not error the frame).
         * That is only for backward compatibility reasons. In the future, these should
         * be an alias to `applyStrategy.bind(null, 'error')` instead.
         */
        error: printStrategyMessage.bind(null, "error")
      }
    });
  }
  if (frame instanceof HttpNetworkFrame && isCommonAssetRequest(frame.data.request)) {
    return;
  }
  return applyStrategy(handle);
}

// node_modules/msw/lib/core/experimental/define-network.mjs
function colorlessPromiseAll(values) {
  const promises = [];
  for (const value of values) {
    if (value instanceof Promise) {
      promises.push(value);
    }
  }
  if (promises.length > 0) {
    return Promise.all(promises).then(() => {
    });
  }
}
var NetworkReadyState = ((NetworkReadyState2) => {
  NetworkReadyState2[NetworkReadyState2["DISABLED"] = 0] = "DISABLED";
  NetworkReadyState2[NetworkReadyState2["ENABLED"] = 1] = "ENABLED";
  return NetworkReadyState2;
})(NetworkReadyState || {});
function defineNetwork(options) {
  let readyState = 0;
  const events = new Emitter();
  const disposable = new Disposable();
  const deriveHandlersController = (handlers) => {
    return handlers instanceof HandlersController ? handlers : new InMemoryHandlersController(handlers || []);
  };
  let resolvedOptions = {
    ...options
  };
  let handlersController = deriveHandlersController(resolvedOptions.handlers);
  return {
    get readyState() {
      return readyState;
    },
    events,
    configure(options2) {
      invariant(
        readyState === 0,
        'Failed to call "configure()" on the network: cannot configure an already enabled network.'
      );
      if (options2.handlers && !Object.is(options2.handlers, resolvedOptions.handlers)) {
        handlersController = deriveHandlersController(options2.handlers);
      }
      resolvedOptions = {
        ...resolvedOptions,
        ...options2
      };
    },
    enable() {
      invariant(
        readyState === 0,
        'Failed to call "enable" on the network: already enabled'
      );
      readyState = 1;
      const session = { active: true };
      disposable["subscriptions"].push(() => {
        session.active = false;
      });
      const result = resolvedOptions.sources.map((source) => {
        NetworkSource.prototype.disable.call(source);
        source.on("frame", async ({ frame }) => {
          frame.events.on("*", (event) => {
            if (!session.active) {
              return;
            }
            events.emit(event);
          });
          const handlers = frame.getHandlers(handlersController);
          await frame.resolve(
            handlers,
            resolvedOptions.onUnhandledFrame || "warn",
            resolvedOptions.context
          );
        });
        return source.enable();
      });
      return colorlessPromiseAll(result);
    },
    disable() {
      invariant(
        readyState === 1,
        'Failed to call "disable" on the network: already disabled'
      );
      readyState = 0;
      disposable.dispose();
      return colorlessPromiseAll(
        resolvedOptions.sources.map((source) => source.disable())
      );
    },
    use(...handlers) {
      handlersController.use(handlers);
    },
    resetHandlers(...handlers) {
      handlersController.reset(handlers);
    },
    restoreHandlers() {
      handlersController.restore();
    },
    listHandlers() {
      return toReadonlyArray(handlersController.currentHandlers());
    }
  };
}

// node_modules/@mswjs/interceptors/lib/browser/hasConfigurableGlobal-C8zq1MCg.mjs
async function emitAsync(emitter, eventName, ...data) {
  const listeners = emitter.listeners(eventName);
  if (listeners.length === 0) return;
  for (const listener of listeners) await listener.apply(emitter, data);
}
var _replacements, _a;
var PatchesRegistry = (_a = class {
  constructor() {
    __privateAdd(this, _replacements, /* @__PURE__ */ new Map());
  }
  applyPatch(owner, key, getNextValue) {
    const ownerReplacements = __privateGet(this, _replacements).get(owner);
    invariant(!(ownerReplacements == null ? void 0 : ownerReplacements.has(key)), `Failed to replace a global value at "${String(key)}": already replaced.`);
    const match = getDeepPropertyDescriptor(owner, key);
    if (typeof match === "undefined") {
      console.warn(`Failed to replace a global value at "${String(key)}": not a global value.`);
      return () => {
      };
    }
    if (match.descriptor.configurable) Object.defineProperty(owner, key, {
      value: getNextValue(owner[key]),
      enumerable: true,
      configurable: true
    });
    else if (match.descriptor.writable) owner[key] = getNextValue(owner[key]);
    else throw new Error(`Failed to patch a non-configurable non-writable property "${key.toString()}"`);
    const restorePatch = () => {
      const currentReplacements = __privateGet(this, _replacements).get(owner);
      if (!(currentReplacements == null ? void 0 : currentReplacements.has(key))) return;
      if (match.owner === owner)
        Object.defineProperty(match.owner, key, match.descriptor);
      else
        Reflect.deleteProperty(owner, key);
      currentReplacements.delete(key);
      if (currentReplacements.size === 0) __privateGet(this, _replacements).delete(owner);
    };
    if (ownerReplacements) ownerReplacements.set(key, restorePatch);
    else __privateGet(this, _replacements).set(owner, /* @__PURE__ */ new Map([[key, restorePatch]]));
    return restorePatch;
  }
  restoreAllPatches() {
    const errors = [];
    for (const [, ownerReplacements] of __privateGet(this, _replacements)) for (const [, restorePatch] of ownerReplacements) try {
      restorePatch();
    } catch (error2) {
      if (error2 instanceof Error) errors.push(error2);
      else throw error2;
    }
    if (errors.length > 0) throw new AggregateError(errors, "FOO!");
  }
}, _replacements = new WeakMap(), _a);
var patchesRegistry = new PatchesRegistry();
function getDeepPropertyDescriptor(owner, key) {
  let currentOwner = owner;
  let descriptor;
  while (currentOwner) {
    descriptor = Object.getOwnPropertyDescriptor(currentOwner, key);
    if (descriptor) return {
      owner: currentOwner,
      descriptor
    };
    currentOwner = Object.getPrototypeOf(currentOwner);
  }
}
function hasConfigurableGlobal(propertyName) {
  const match = getDeepPropertyDescriptor(globalThis, propertyName);
  if (typeof match === "undefined") return false;
  const { descriptor } = match;
  if (typeof descriptor.get === "function" && typeof descriptor.get() === "undefined") return false;
  if (typeof descriptor.get === "undefined" && descriptor.value == null) return false;
  if (typeof descriptor.set === "undefined" && !descriptor.configurable) {
    console.error(`[MSW] Failed to apply interceptor: the global \`${propertyName}\` property is non-configurable. This is likely an issue with your environment. If you are using a framework, please open an issue about this in their repository.`);
    return false;
  }
  return true;
}

// node_modules/@mswjs/interceptors/lib/browser/interceptors/WebSocket/index.mjs
function bindEvent(target, event) {
  Object.defineProperties(event, {
    target: {
      value: target,
      enumerable: true,
      writable: true
    },
    currentTarget: {
      value: target,
      enumerable: true,
      writable: true
    }
  });
  return event;
}
var kCancelable = Symbol("kCancelable");
var kDefaultPrevented = Symbol("kDefaultPrevented");
var CancelableMessageEvent = class extends MessageEvent {
  constructor(type, init) {
    super(type, init);
    this[kCancelable] = !!init.cancelable;
    this[kDefaultPrevented] = false;
  }
  get cancelable() {
    return this[kCancelable];
  }
  set cancelable(nextCancelable) {
    this[kCancelable] = nextCancelable;
  }
  get defaultPrevented() {
    return this[kDefaultPrevented];
  }
  set defaultPrevented(nextDefaultPrevented) {
    this[kDefaultPrevented] = nextDefaultPrevented;
  }
  preventDefault() {
    if (this.cancelable && !this[kDefaultPrevented]) this[kDefaultPrevented] = true;
  }
};
var CloseEvent = class extends Event {
  constructor(type, init = {}) {
    super(type, init);
    this.code = init.code === void 0 ? 0 : init.code;
    this.reason = init.reason === void 0 ? "" : init.reason;
    this.wasClean = init.wasClean === void 0 ? false : init.wasClean;
  }
};
var CancelableCloseEvent = class extends CloseEvent {
  constructor(type, init = {}) {
    super(type, init);
    this[kCancelable] = !!init.cancelable;
    this[kDefaultPrevented] = false;
  }
  get cancelable() {
    return this[kCancelable];
  }
  set cancelable(nextCancelable) {
    this[kCancelable] = nextCancelable;
  }
  get defaultPrevented() {
    return this[kDefaultPrevented];
  }
  set defaultPrevented(nextDefaultPrevented) {
    this[kDefaultPrevented] = nextDefaultPrevented;
  }
  preventDefault() {
    if (this.cancelable && !this[kDefaultPrevented]) this[kDefaultPrevented] = true;
  }
};
var kEmitter$1 = Symbol("kEmitter");
var kBoundListener$1 = Symbol("kBoundListener");
var WebSocketClientConnection = class {
  constructor(socket, transport) {
    this.socket = socket;
    this.transport = transport;
    this.id = createRequestId();
    this.url = new URL(socket.url);
    this[kEmitter$1] = new EventTarget();
    this.transport.addEventListener("outgoing", (event) => {
      const message = bindEvent(this.socket, new CancelableMessageEvent("message", {
        data: event.data,
        origin: event.origin,
        cancelable: true
      }));
      this[kEmitter$1].dispatchEvent(message);
      if (message.defaultPrevented) event.preventDefault();
    });
    this.transport.addEventListener("close", (event) => {
      this[kEmitter$1].dispatchEvent(bindEvent(this.socket, new CloseEvent("close", event)));
    });
  }
  /**
  * Listen for the outgoing events from the connected WebSocket client.
  */
  addEventListener(type, listener, options) {
    if (!Reflect.has(listener, kBoundListener$1)) {
      const boundListener = listener.bind(this.socket);
      Object.defineProperty(listener, kBoundListener$1, {
        value: boundListener,
        enumerable: false,
        configurable: false
      });
    }
    this[kEmitter$1].addEventListener(type, Reflect.get(listener, kBoundListener$1), options);
  }
  /**
  * Removes the listener for the given event.
  */
  removeEventListener(event, listener, options) {
    this[kEmitter$1].removeEventListener(event, Reflect.get(listener, kBoundListener$1), options);
  }
  /**
  * Send data to the connected client.
  */
  send(data) {
    this.transport.send(data);
  }
  /**
  * Close the WebSocket connection.
  * @param {number} code A status code (see https://www.rfc-editor.org/rfc/rfc6455#section-7.4.1).
  * @param {string} reason A custom connection close reason.
  */
  close(code, reason) {
    this.transport.close(code, reason);
  }
};
var WEBSOCKET_CLOSE_CODE_RANGE_ERROR = "InvalidAccessError: close code out of user configurable range";
var kPassthroughPromise = Symbol("kPassthroughPromise");
var kOnSend = Symbol("kOnSend");
var kClose = Symbol("kClose");
var _a2;
var WebSocketOverride = (_a2 = class extends EventTarget {
  constructor(url, protocols) {
    super();
    this.CONNECTING = 0;
    this.OPEN = 1;
    this.CLOSING = 2;
    this.CLOSED = 3;
    this._onopen = null;
    this._onmessage = null;
    this._onerror = null;
    this._onclose = null;
    this.url = resolveWebSocketUrl(url);
    this.protocol = "";
    this.extensions = "";
    this.binaryType = "blob";
    this.readyState = this.CONNECTING;
    this.bufferedAmount = 0;
    this[kPassthroughPromise] = new DeferredPromise();
    queueMicrotask(async () => {
      if (await this[kPassthroughPromise]) return;
      this.protocol = typeof protocols === "string" ? protocols : Array.isArray(protocols) && protocols.length > 0 ? protocols[0] : "";
      if (this.readyState === this.CONNECTING) {
        this.readyState = this.OPEN;
        this.dispatchEvent(bindEvent(this, new Event("open")));
      }
    });
  }
  set onopen(listener) {
    this.removeEventListener("open", this._onopen);
    this._onopen = listener;
    if (listener !== null) this.addEventListener("open", listener);
  }
  get onopen() {
    return this._onopen;
  }
  set onmessage(listener) {
    this.removeEventListener("message", this._onmessage);
    this._onmessage = listener;
    if (listener !== null) this.addEventListener("message", listener);
  }
  get onmessage() {
    return this._onmessage;
  }
  set onerror(listener) {
    this.removeEventListener("error", this._onerror);
    this._onerror = listener;
    if (listener !== null) this.addEventListener("error", listener);
  }
  get onerror() {
    return this._onerror;
  }
  set onclose(listener) {
    this.removeEventListener("close", this._onclose);
    this._onclose = listener;
    if (listener !== null) this.addEventListener("close", listener);
  }
  get onclose() {
    return this._onclose;
  }
  /**
  * @see https://websockets.spec.whatwg.org/#ref-for-dom-websocket-send%E2%91%A0
  */
  send(data) {
    if (this.readyState === this.CONNECTING) {
      this.close();
      throw new DOMException("InvalidStateError");
    }
    if (this.readyState === this.CLOSING || this.readyState === this.CLOSED) return;
    this.bufferedAmount += getDataSize(data);
    queueMicrotask(() => {
      var _a20;
      this.bufferedAmount = 0;
      (_a20 = this[kOnSend]) == null ? void 0 : _a20.call(this, data);
    });
  }
  close(code = 1e3, reason) {
    invariant(code, WEBSOCKET_CLOSE_CODE_RANGE_ERROR);
    invariant(code === 1e3 || code >= 3e3 && code <= 4999, WEBSOCKET_CLOSE_CODE_RANGE_ERROR);
    this[kClose](code, reason);
  }
  [kClose](code = 1e3, reason, wasClean = true) {
    if (this.readyState === this.CLOSING || this.readyState === this.CLOSED) return;
    this.readyState = this.CLOSING;
    queueMicrotask(() => {
      this.readyState = this.CLOSED;
      this.dispatchEvent(bindEvent(this, new CloseEvent("close", {
        code,
        reason,
        wasClean
      })));
      this._onopen = null;
      this._onmessage = null;
      this._onerror = null;
      this._onclose = null;
    });
  }
  addEventListener(type, listener, options) {
    return super.addEventListener(type, listener, options);
  }
  removeEventListener(type, callback, options) {
    return super.removeEventListener(type, callback, options);
  }
}, _a2.CONNECTING = 0, _a2.OPEN = 1, _a2.CLOSING = 2, _a2.CLOSED = 3, _a2);
function getDataSize(data) {
  if (typeof data === "string") return data.length;
  if (data instanceof Blob) return data.size;
  return data.byteLength;
}
var kEmitter = Symbol("kEmitter");
var kBoundListener = Symbol("kBoundListener");
var kSend = Symbol("kSend");
var WebSocketServerConnection = class {
  constructor(client, transport, createConnection) {
    this.client = client;
    this.transport = transport;
    this.createConnection = createConnection;
    this[kEmitter] = new EventTarget();
    this.mockCloseController = new AbortController();
    this.realCloseController = new AbortController();
    this.transport.addEventListener("outgoing", (event) => {
      if (typeof this.realWebSocket === "undefined") return;
      queueMicrotask(() => {
        if (!event.defaultPrevented)
          this[kSend](event.data);
      });
    });
    this.transport.addEventListener("incoming", this.handleIncomingMessage.bind(this));
  }
  /**
  * The `WebSocket` instance connected to the original server.
  * Accessing this before calling `server.connect()` will throw.
  */
  get socket() {
    invariant(this.realWebSocket, 'Cannot access "socket" on the original WebSocket server object: the connection is not open. Did you forget to call `server.connect()`?');
    return this.realWebSocket;
  }
  /**
  * Open connection to the original WebSocket server.
  */
  connect() {
    invariant(!this.realWebSocket || this.realWebSocket.readyState !== WebSocket.OPEN, 'Failed to call "connect()" on the original WebSocket instance: the connection already open');
    const realWebSocket = this.createConnection();
    realWebSocket.binaryType = this.client.binaryType;
    realWebSocket.addEventListener("open", (event) => {
      this[kEmitter].dispatchEvent(bindEvent(this.realWebSocket, new Event("open", event)));
    }, { once: true });
    realWebSocket.addEventListener("message", (event) => {
      this.transport.dispatchEvent(bindEvent(this.realWebSocket, new MessageEvent("incoming", {
        data: event.data,
        origin: event.origin
      })));
    });
    this.client.addEventListener("close", (event) => {
      this.handleMockClose(event);
    }, { signal: this.mockCloseController.signal });
    realWebSocket.addEventListener("close", (event) => {
      this.handleRealClose(event);
    }, { signal: this.realCloseController.signal });
    realWebSocket.addEventListener("error", () => {
      const errorEvent = bindEvent(realWebSocket, new Event("error", { cancelable: true }));
      this[kEmitter].dispatchEvent(errorEvent);
      if (!errorEvent.defaultPrevented) this.client.dispatchEvent(bindEvent(this.client, new Event("error")));
    });
    this.realWebSocket = realWebSocket;
  }
  /**
  * Listen for the incoming events from the original WebSocket server.
  */
  addEventListener(event, listener, options) {
    if (!Reflect.has(listener, kBoundListener)) {
      const boundListener = listener.bind(this.client);
      Object.defineProperty(listener, kBoundListener, {
        value: boundListener,
        enumerable: false
      });
    }
    this[kEmitter].addEventListener(event, Reflect.get(listener, kBoundListener), options);
  }
  /**
  * Remove the listener for the given event.
  */
  removeEventListener(event, listener, options) {
    this[kEmitter].removeEventListener(event, Reflect.get(listener, kBoundListener), options);
  }
  /**
  * Send data to the original WebSocket server.
  * @example
  * server.send('hello')
  * server.send(new Blob(['hello']))
  * server.send(new TextEncoder().encode('hello'))
  */
  send(data) {
    this[kSend](data);
  }
  [kSend](data) {
    const { realWebSocket } = this;
    invariant(realWebSocket, 'Failed to call "server.send()" for "%s": the connection is not open. Did you forget to call "server.connect()"?', this.client.url);
    if (realWebSocket.readyState === WebSocket.CLOSING || realWebSocket.readyState === WebSocket.CLOSED) return;
    if (realWebSocket.readyState === WebSocket.CONNECTING) {
      realWebSocket.addEventListener("open", () => {
        realWebSocket.send(data);
      }, { once: true });
      return;
    }
    realWebSocket.send(data);
  }
  /**
  * Close the actual server connection.
  */
  close() {
    const { realWebSocket } = this;
    invariant(realWebSocket, 'Failed to close server connection for "%s": the connection is not open. Did you forget to call "server.connect()"?', this.client.url);
    this.realCloseController.abort();
    if (realWebSocket.readyState === WebSocket.CLOSING || realWebSocket.readyState === WebSocket.CLOSED) return;
    realWebSocket.close();
    queueMicrotask(() => {
      this[kEmitter].dispatchEvent(bindEvent(this.realWebSocket, new CancelableCloseEvent("close", {
        code: 1e3,
        cancelable: true
      })));
    });
  }
  handleIncomingMessage(event) {
    const messageEvent = bindEvent(event.target, new CancelableMessageEvent("message", {
      data: event.data,
      origin: event.origin,
      cancelable: true
    }));
    this[kEmitter].dispatchEvent(messageEvent);
    if (!messageEvent.defaultPrevented) this.client.dispatchEvent(bindEvent(
      /**
      * @note Bind the forwarded original server events
      * to the mock WebSocket instance so it would
      * dispatch them straight away.
      */
      this.client,
      new MessageEvent("message", {
        data: event.data,
        origin: event.origin
      })
    ));
  }
  handleMockClose(_event2) {
    if (this.realWebSocket) this.realWebSocket.close();
  }
  handleRealClose(event) {
    this.mockCloseController.abort();
    const closeEvent = bindEvent(this.realWebSocket, new CancelableCloseEvent("close", {
      code: event.code,
      reason: event.reason,
      wasClean: event.wasClean,
      cancelable: true
    }));
    this[kEmitter].dispatchEvent(closeEvent);
    if (!closeEvent.defaultPrevented) this.client[kClose](event.code, event.reason);
  }
};
var WebSocketClassTransport = class extends EventTarget {
  constructor(socket) {
    super();
    this.socket = socket;
    this.socket.addEventListener("close", (event) => {
      this.dispatchEvent(bindEvent(this.socket, new CloseEvent("close", event)));
    });
    this.socket[kOnSend] = (data) => {
      this.dispatchEvent(bindEvent(this.socket, new CancelableMessageEvent("outgoing", {
        data,
        origin: this.socket.url,
        cancelable: true
      })));
    };
  }
  addEventListener(type, callback, options) {
    return super.addEventListener(type, callback, options);
  }
  dispatchEvent(event) {
    return super.dispatchEvent(event);
  }
  send(data) {
    queueMicrotask(() => {
      if (this.socket.readyState === this.socket.CLOSING || this.socket.readyState === this.socket.CLOSED) return;
      const dispatchEvent = () => {
        this.socket.dispatchEvent(bindEvent(
          /**
          * @note Setting this event's "target" to the
          * WebSocket override instance is important.
          * This way it can tell apart original incoming events
          * (must be forwarded to the transport) from the
          * mocked message events like the one below
          * (must be dispatched on the client instance).
          */
          this.socket,
          new MessageEvent("message", {
            data,
            origin: this.socket.url
          })
        ));
      };
      if (this.socket.readyState === this.socket.CONNECTING) this.socket.addEventListener("open", () => {
        dispatchEvent();
      }, { once: true });
      else dispatchEvent();
    });
  }
  close(code, reason) {
    this.socket[kClose](code, reason);
  }
};
var _a3;
var WebSocketInterceptor = (_a3 = class extends Interceptor {
  constructor() {
    super(_a3.symbol);
  }
  checkEnvironment() {
    return hasConfigurableGlobal("WebSocket");
  }
  setup() {
    const logger = this.logger.extend("setup");
    const WebSocketProxy = new Proxy(globalThis.WebSocket, { construct: (target, args, newTarget) => {
      const [url, protocols] = args;
      const createConnection = () => {
        return Reflect.construct(target, args, newTarget);
      };
      const socket = new WebSocketOverride(url, protocols);
      const transport = new WebSocketClassTransport(socket);
      queueMicrotask(async () => {
        try {
          const server = new WebSocketServerConnection(socket, transport, createConnection);
          const hasConnectionListeners = this.emitter.listenerCount("connection") > 0;
          await emitAsync(this.emitter, "connection", {
            client: new WebSocketClientConnection(socket, transport),
            server,
            info: { protocols }
          });
          if (hasConnectionListeners) socket[kPassthroughPromise].resolve(false);
          else {
            socket[kPassthroughPromise].resolve(true);
            server.connect();
            server.addEventListener("open", () => {
              socket.dispatchEvent(bindEvent(socket, new Event("open")));
              if (server["realWebSocket"]) socket.protocol = server["realWebSocket"].protocol;
            });
          }
        } catch (error2) {
          if (error2 instanceof Error) {
            socket.dispatchEvent(new Event("error"));
            if (socket.readyState !== WebSocket.CLOSING && socket.readyState !== WebSocket.CLOSED) socket[kClose](1011, error2.message, false);
            console.error(error2);
          }
        }
      });
      return socket;
    } });
    logger.info("patching global WebSocket...");
    this.subscriptions.push(patchesRegistry.applyPatch(globalThis, "WebSocket", () => WebSocketProxy));
    logger.info("global WebSocket patched!", globalThis.WebSocket.name);
  }
}, _a3.symbol = Symbol.for("websocket-interceptor"), _a3);

// node_modules/msw/lib/core/experimental/frames/websocket-frame.mjs
var WebSocketConnectionEvent = class extends TypedEvent {
  constructor(type, data) {
    super(...[type, {}]);
    __publicField(this, "url");
    __publicField(this, "protocols");
    this.url = data.url;
    this.protocols = data.protocols;
  }
};
var UnhandledWebSocketExceptionEvent = class extends TypedEvent {
  constructor(type, data) {
    super(...[type, {}]);
    __publicField(this, "url");
    __publicField(this, "protocols");
    __publicField(this, "error");
    this.url = data.url;
    this.protocols = data.protocols;
    this.error = data.error;
  }
};
var WebSocketNetworkFrame = class extends NetworkFrame {
  constructor(options) {
    super("ws", {
      connection: options.connection
    });
  }
  getHandlers(controller) {
    return controller.getHandlersByKind("websocket");
  }
  async resolve(handlers, onUnhandledFrame, resolutionContext) {
    var _a20;
    const { connection } = this.data;
    this.events.emit(
      new WebSocketConnectionEvent("connection", {
        url: connection.client.url,
        protocols: connection.info.protocols
      })
    );
    if (handlers.length === 0) {
      await executeUnhandledFrameHandle(this, onUnhandledFrame).then(
        () => this.passthrough(),
        (error2) => this.errorWith(error2)
      );
      return false;
    }
    let hasMatchingHandlers = false;
    for (const handler of handlers) {
      const handlerConnection = await handler.run(connection, {
        baseUrl: (_a20 = resolutionContext == null ? void 0 : resolutionContext.baseUrl) == null ? void 0 : _a20.toString(),
        /**
         * @note Do not emit the "connection" event when running the handler.
         * Use the run only to get the resolved connection object.
         */
        [kAutoConnect]: false
      });
      if (!handlerConnection) {
        continue;
      }
      hasMatchingHandlers = true;
      const removeLogger = !(resolutionContext == null ? void 0 : resolutionContext.quiet) ? handler.log(connection) : void 0;
      try {
        if (!handler[kConnect](handlerConnection)) {
          removeLogger == null ? void 0 : removeLogger();
        }
      } catch (error2) {
        if (!this.events.emit(
          new UnhandledWebSocketExceptionEvent("unhandledException", {
            error: error2,
            url: connection.client.url,
            protocols: connection.info.protocols
          })
        )) {
          console.error(error2);
          devUtils.error(
            'Encountered an unhandled exception during the handler lookup for "%s". Please see the original error above.',
            connection.client.url
          );
        }
        throw error2;
      }
    }
    if (!hasMatchingHandlers) {
      await executeUnhandledFrameHandle(this, onUnhandledFrame).then(
        () => this.passthrough(),
        (error2) => this.errorWith(error2)
      );
      return false;
    }
    return true;
  }
  async getUnhandledMessage() {
    const { connection } = this.data;
    const details = `

  • ${connection.client.url}

`;
    return `intercepted a WebSocket connection without a matching event handler:${details}If you still wish to intercept this unhandled connection, please create an event handler for it.
Read more: https://mswjs.io/docs/websocket`;
  }
};

// node_modules/msw/lib/core/experimental/sources/interceptor-source.mjs
var _interceptor, _frames, _InterceptorSource_instances, handleRequest_fn, handleResponse_fn, handleWebSocketConnection_fn;
var InterceptorSource = class extends NetworkSource {
  constructor(options) {
    super();
    __privateAdd(this, _InterceptorSource_instances);
    __privateAdd(this, _interceptor);
    __privateAdd(this, _frames);
    __privateSet(this, _interceptor, new BatchInterceptor({
      name: "interceptor-source",
      interceptors: options.interceptors
    }));
    __privateSet(this, _frames, /* @__PURE__ */ new Map());
  }
  enable() {
    __privateGet(this, _interceptor).apply();
    __privateGet(this, _interceptor).on("request", __privateMethod(this, _InterceptorSource_instances, handleRequest_fn).bind(this)).on("response", __privateMethod(this, _InterceptorSource_instances, handleResponse_fn).bind(this)).on("connection", __privateMethod(this, _InterceptorSource_instances, handleWebSocketConnection_fn).bind(this));
  }
  disable() {
    super.disable();
    __privateGet(this, _interceptor).dispose();
    __privateGet(this, _frames).clear();
  }
};
_interceptor = new WeakMap();
_frames = new WeakMap();
_InterceptorSource_instances = new WeakSet();
handleRequest_fn = async function({
  requestId,
  request,
  controller
}) {
  const httpFrame = new InterceptorHttpNetworkFrame({
    id: requestId,
    request,
    controller
  });
  __privateGet(this, _frames).set(requestId, httpFrame);
  await this.queue(httpFrame);
};
handleResponse_fn = async function({
  requestId,
  request,
  response,
  isMockedResponse
}) {
  const httpFrame = __privateGet(this, _frames).get(requestId);
  __privateGet(this, _frames).delete(requestId);
  if (httpFrame == null) {
    return;
  }
  queueMicrotask(() => {
    try {
      httpFrame.events.emit(
        new ResponseEvent(
          isMockedResponse ? "response:mocked" : "response:bypass",
          {
            requestId,
            request,
            response
          }
        )
      );
    } finally {
      httpFrame.events.removeAllListeners();
    }
  });
};
handleWebSocketConnection_fn = async function(connection) {
  await this.queue(
    new InterceptorWebSocketNetworkFrame({
      connection
    })
  );
};
var _controller;
var InterceptorHttpNetworkFrame = class extends HttpNetworkFrame {
  constructor(options) {
    super({
      id: options.id,
      request: options.request
    });
    __privateAdd(this, _controller);
    __privateSet(this, _controller, options.controller);
  }
  passthrough() {
    deleteRequestPassthroughHeader(this.data.request);
  }
  respondWith(response) {
    if (response) {
      __privateGet(this, _controller).respondWith(response);
    }
  }
  errorWith(reason) {
    if (reason instanceof Response) {
      return this.respondWith(reason);
    }
    if (reason instanceof InternalError) {
      __privateGet(this, _controller).errorWith(reason);
    }
    throw reason;
  }
};
_controller = new WeakMap();
var InterceptorWebSocketNetworkFrame = class extends WebSocketNetworkFrame {
  constructor(args) {
    super({ connection: args.connection });
    args.connection.client.addEventListener(
      "close",
      () => {
        this.events.removeAllListeners();
      },
      {
        once: true
      }
    );
  }
  errorWith(reason) {
    if (reason instanceof Error) {
      const { client } = this.data.connection;
      const errorEvent = new Event("error");
      Object.defineProperty(errorEvent, "cause", {
        enumerable: true,
        configurable: false,
        value: reason
      });
      client.socket.dispatchEvent(errorEvent);
    }
  }
  passthrough() {
    this.data.connection.server.connect();
  }
};

// node_modules/msw/lib/core/experimental/compat.mjs
function fromLegacyOnUnhandledRequest(getLegacyValue) {
  return ({ frame, defaults }) => {
    const legacyOnUnhandledRequestStrategy = getLegacyValue();
    if (legacyOnUnhandledRequestStrategy == null) {
      return;
    }
    if (typeof legacyOnUnhandledRequestStrategy === "function") {
      const request = frame instanceof HttpNetworkFrame ? frame.data.request : frame instanceof WebSocketNetworkFrame ? new Request(frame.data.connection.client.url, {
        headers: {
          connection: "upgrade",
          upgrade: "websocket"
        }
      }) : null;
      invariant(
        request != null,
        'Failed to coerce a network frame to a legacy `onUnhandledRequest` strategy: unknown frame protocol "%s"',
        frame.protocol
      );
      return legacyOnUnhandledRequestStrategy(request, {
        warning: defaults.warn,
        error: defaults.error
      });
    }
    return executeUnhandledFrameHandle(frame, legacyOnUnhandledRequestStrategy);
  };
}

// node_modules/msw/lib/core/utils/toResponseInit.mjs
function toResponseInit(response) {
  return {
    status: response.status,
    statusText: response.statusText,
    headers: Object.fromEntries(response.headers.entries())
  };
}

// node_modules/msw/lib/browser/index.mjs
var POSITIONALS_EXP = /(%?)(%([sdijo]))/g;
function serializePositional(positional, flag) {
  switch (flag) {
    case "s":
      return positional;
    case "d":
    case "i":
      return Number(positional);
    case "j":
      return JSON.stringify(positional);
    case "o": {
      if (typeof positional === "string") {
        return positional;
      }
      const json = JSON.stringify(positional);
      if (json === "{}" || json === "[]" || /^\[object .+?\]$/.test(json)) {
        return positional;
      }
      return json;
    }
  }
}
function format(message, ...positionals) {
  if (positionals.length === 0) {
    return message;
  }
  let positionalIndex = 0;
  let formattedMessage = message.replace(
    POSITIONALS_EXP,
    (match, isEscaped, _, flag) => {
      const positional = positionals[positionalIndex];
      const value = serializePositional(positional, flag);
      if (!isEscaped) {
        positionalIndex++;
        return value;
      }
      return match;
    }
  );
  if (positionalIndex < positionals.length) {
    formattedMessage += ` ${positionals.slice(positionalIndex).join(" ")}`;
  }
  formattedMessage = formattedMessage.replace(/%{2,2}/g, "%");
  return formattedMessage;
}
var STACK_FRAMES_TO_IGNORE = 2;
function cleanErrorStack(error2) {
  if (!error2.stack) {
    return;
  }
  const nextStack = error2.stack.split("\n");
  nextStack.splice(1, STACK_FRAMES_TO_IGNORE);
  error2.stack = nextStack.join("\n");
}
var InvariantError = class extends Error {
  constructor(message, ...positionals) {
    super(message);
    this.message = message;
    this.name = "Invariant Violation";
    this.message = format(message, ...positionals);
    cleanErrorStack(this);
  }
};
var invariant2 = (predicate, message, ...positionals) => {
  if (!predicate) {
    throw new InvariantError(message, ...positionals);
  }
};
invariant2.as = (ErrorConstructor, predicate, message, ...positionals) => {
  if (!predicate) {
    const formatMessage = positionals.length === 0 ? message : format(message, ...positionals);
    let error2;
    try {
      error2 = Reflect.construct(ErrorConstructor, [
        formatMessage
      ]);
    } catch (err) {
      error2 = ErrorConstructor(formatMessage);
    }
    throw error2;
  }
};
function isNodeProcess() {
  if (typeof navigator !== "undefined" && navigator.product === "ReactNative") {
    return true;
  }
  if (typeof process !== "undefined") {
    const type = process.type;
    if (type === "renderer" || type === "worker") {
      return false;
    }
    return !!(process.versions && process.versions.node);
  }
  return false;
}
var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var colors_exports = {};
__export(colors_exports, {
  blue: () => blue,
  gray: () => gray,
  green: () => green,
  red: () => red,
  yellow: () => yellow
});
function yellow(text) {
  return `\x1B[33m${text}\x1B[0m`;
}
function blue(text) {
  return `\x1B[34m${text}\x1B[0m`;
}
function gray(text) {
  return `\x1B[90m${text}\x1B[0m`;
}
function red(text) {
  return `\x1B[31m${text}\x1B[0m`;
}
function green(text) {
  return `\x1B[32m${text}\x1B[0m`;
}
var IS_NODE = isNodeProcess();
var Logger = class {
  constructor(name) {
    __publicField(this, "prefix");
    this.name = name;
    this.prefix = `[${this.name}]`;
    const LOGGER_NAME = getVariable("DEBUG");
    const LOGGER_LEVEL = getVariable("LOG_LEVEL");
    const isLoggingEnabled = LOGGER_NAME === "1" || LOGGER_NAME === "true" || typeof LOGGER_NAME !== "undefined" && this.name.startsWith(LOGGER_NAME);
    if (isLoggingEnabled) {
      this.debug = isDefinedAndNotEquals(LOGGER_LEVEL, "debug") ? noop : this.debug;
      this.info = isDefinedAndNotEquals(LOGGER_LEVEL, "info") ? noop : this.info;
      this.success = isDefinedAndNotEquals(LOGGER_LEVEL, "success") ? noop : this.success;
      this.warning = isDefinedAndNotEquals(LOGGER_LEVEL, "warning") ? noop : this.warning;
      this.error = isDefinedAndNotEquals(LOGGER_LEVEL, "error") ? noop : this.error;
    } else {
      this.info = noop;
      this.success = noop;
      this.warning = noop;
      this.error = noop;
      this.only = noop;
    }
  }
  extend(domain) {
    return new Logger(`${this.name}:${domain}`);
  }
  /**
   * Print a debug message.
   * @example
   * logger.debug('no duplicates found, creating a document...')
   */
  debug(message, ...positionals) {
    this.logEntry({
      level: "debug",
      message: gray(message),
      positionals,
      prefix: this.prefix,
      colors: {
        prefix: "gray"
      }
    });
  }
  /**
   * Print an info message.
   * @example
   * logger.info('start parsing...')
   */
  info(message, ...positionals) {
    this.logEntry({
      level: "info",
      message,
      positionals,
      prefix: this.prefix,
      colors: {
        prefix: "blue"
      }
    });
    const performance2 = new PerformanceEntry();
    return (message2, ...positionals2) => {
      performance2.measure();
      this.logEntry({
        level: "info",
        message: `${message2} ${gray(`${performance2.deltaTime}ms`)}`,
        positionals: positionals2,
        prefix: this.prefix,
        colors: {
          prefix: "blue"
        }
      });
    };
  }
  /**
   * Print a success message.
   * @example
   * logger.success('successfully created document')
   */
  success(message, ...positionals) {
    this.logEntry({
      level: "info",
      message,
      positionals,
      prefix: `✔ ${this.prefix}`,
      colors: {
        timestamp: "green",
        prefix: "green"
      }
    });
  }
  /**
   * Print a warning.
   * @example
   * logger.warning('found legacy document format')
   */
  warning(message, ...positionals) {
    this.logEntry({
      level: "warning",
      message,
      positionals,
      prefix: `⚠ ${this.prefix}`,
      colors: {
        timestamp: "yellow",
        prefix: "yellow"
      }
    });
  }
  /**
   * Print an error message.
   * @example
   * logger.error('something went wrong')
   */
  error(message, ...positionals) {
    this.logEntry({
      level: "error",
      message,
      positionals,
      prefix: `✖ ${this.prefix}`,
      colors: {
        timestamp: "red",
        prefix: "red"
      }
    });
  }
  /**
   * Execute the given callback only when the logging is enabled.
   * This is skipped in its entirety and has no runtime cost otherwise.
   * This executes regardless of the log level.
   * @example
   * logger.only(() => {
   *   logger.info('additional info')
   * })
   */
  only(callback) {
    callback();
  }
  createEntry(level, message) {
    return {
      timestamp: /* @__PURE__ */ new Date(),
      level,
      message
    };
  }
  logEntry(args) {
    const {
      level,
      message,
      prefix,
      colors: customColors,
      positionals = []
    } = args;
    const entry = this.createEntry(level, message);
    const timestampColor = (customColors == null ? void 0 : customColors.timestamp) || "gray";
    const prefixColor = (customColors == null ? void 0 : customColors.prefix) || "gray";
    const colorize = {
      timestamp: colors_exports[timestampColor],
      prefix: colors_exports[prefixColor]
    };
    const write = this.getWriter(level);
    write(
      [colorize.timestamp(this.formatTimestamp(entry.timestamp))].concat(prefix != null ? colorize.prefix(prefix) : []).concat(serializeInput(message)).join(" "),
      ...positionals.map(serializeInput)
    );
  }
  formatTimestamp(timestamp) {
    return `${timestamp.toLocaleTimeString(
      "en-GB"
    )}:${timestamp.getMilliseconds()}`;
  }
  getWriter(level) {
    switch (level) {
      case "debug":
      case "success":
      case "info": {
        return log;
      }
      case "warning": {
        return warn;
      }
      case "error": {
        return error;
      }
    }
  }
};
var PerformanceEntry = class {
  constructor() {
    __publicField(this, "startTime");
    __publicField(this, "endTime");
    __publicField(this, "deltaTime");
    this.startTime = performance.now();
  }
  measure() {
    this.endTime = performance.now();
    const deltaTime = this.endTime - this.startTime;
    this.deltaTime = deltaTime.toFixed(2);
  }
};
var noop = () => void 0;
function log(message, ...positionals) {
  if (IS_NODE) {
    process.stdout.write(format(message, ...positionals) + "\n");
    return;
  }
  console.log(message, ...positionals);
}
function warn(message, ...positionals) {
  if (IS_NODE) {
    process.stderr.write(format(message, ...positionals) + "\n");
    return;
  }
  console.warn(message, ...positionals);
}
function error(message, ...positionals) {
  if (IS_NODE) {
    process.stderr.write(format(message, ...positionals) + "\n");
    return;
  }
  console.error(message, ...positionals);
}
function getVariable(variableName) {
  var _a20;
  if (IS_NODE) {
    return process.env[variableName];
  }
  return (_a20 = globalThis[variableName]) == null ? void 0 : _a20.toString();
}
function isDefinedAndNotEquals(value, expected) {
  return value !== void 0 && value !== expected;
}
function serializeInput(message) {
  if (typeof message === "undefined") {
    return "undefined";
  }
  if (message === null) {
    return "null";
  }
  if (typeof message === "string") {
    return message;
  }
  if (typeof message === "object") {
    return JSON.stringify(message);
  }
  return message.toString();
}
var MemoryLeakError = class extends Error {
  constructor(emitter, type, count) {
    super(
      `Possible EventEmitter memory leak detected. ${count} ${type.toString()} listeners added. Use emitter.setMaxListeners() to increase limit`
    );
    this.emitter = emitter;
    this.type = type;
    this.count = count;
    this.name = "MaxListenersExceededWarning";
  }
};
var _Emitter = class {
  static listenerCount(emitter, eventName) {
    return emitter.listenerCount(eventName);
  }
  constructor() {
    this.events = /* @__PURE__ */ new Map();
    this.maxListeners = _Emitter.defaultMaxListeners;
    this.hasWarnedAboutPotentialMemoryLeak = false;
  }
  _emitInternalEvent(internalEventName, eventName, listener) {
    this.emit(
      internalEventName,
      ...[eventName, listener]
    );
  }
  _getListeners(eventName) {
    return Array.prototype.concat.apply([], this.events.get(eventName)) || [];
  }
  _removeListener(listeners, listener) {
    const index = listeners.indexOf(listener);
    if (index > -1) {
      listeners.splice(index, 1);
    }
    return [];
  }
  _wrapOnceListener(eventName, listener) {
    const onceListener = (...data) => {
      this.removeListener(eventName, onceListener);
      return listener.apply(this, data);
    };
    Object.defineProperty(onceListener, "name", { value: listener.name });
    return onceListener;
  }
  setMaxListeners(maxListeners) {
    this.maxListeners = maxListeners;
    return this;
  }
  /**
   * Returns the current max listener value for the `Emitter` which is
   * either set by `emitter.setMaxListeners(n)` or defaults to
   * `Emitter.defaultMaxListeners`.
   */
  getMaxListeners() {
    return this.maxListeners;
  }
  /**
   * Returns an array listing the events for which the emitter has registered listeners.
   * The values in the array will be strings or Symbols.
   */
  eventNames() {
    return Array.from(this.events.keys());
  }
  /**
   * Synchronously calls each of the listeners registered for the event named `eventName`,
   * in the order they were registered, passing the supplied arguments to each.
   * Returns `true` if the event has listeners, `false` otherwise.
   *
   * @example
   * const emitter = new Emitter<{ hello: [string] }>()
   * emitter.emit('hello', 'John')
   */
  emit(eventName, ...data) {
    const listeners = this._getListeners(eventName);
    listeners.forEach((listener) => {
      listener.apply(this, data);
    });
    return listeners.length > 0;
  }
  addListener(eventName, listener) {
    this._emitInternalEvent("newListener", eventName, listener);
    const nextListeners = this._getListeners(eventName).concat(listener);
    this.events.set(eventName, nextListeners);
    if (this.maxListeners > 0 && this.listenerCount(eventName) > this.maxListeners && !this.hasWarnedAboutPotentialMemoryLeak) {
      this.hasWarnedAboutPotentialMemoryLeak = true;
      const memoryLeakWarning = new MemoryLeakError(
        this,
        eventName,
        this.listenerCount(eventName)
      );
      console.warn(memoryLeakWarning);
    }
    return this;
  }
  on(eventName, listener) {
    return this.addListener(eventName, listener);
  }
  once(eventName, listener) {
    return this.addListener(
      eventName,
      this._wrapOnceListener(eventName, listener)
    );
  }
  prependListener(eventName, listener) {
    const listeners = this._getListeners(eventName);
    if (listeners.length > 0) {
      const nextListeners = [listener].concat(listeners);
      this.events.set(eventName, nextListeners);
    } else {
      this.events.set(eventName, listeners.concat(listener));
    }
    return this;
  }
  prependOnceListener(eventName, listener) {
    return this.prependListener(
      eventName,
      this._wrapOnceListener(eventName, listener)
    );
  }
  removeListener(eventName, listener) {
    const listeners = this._getListeners(eventName);
    if (listeners.length > 0) {
      this._removeListener(listeners, listener);
      this.events.set(eventName, listeners);
      this._emitInternalEvent("removeListener", eventName, listener);
    }
    return this;
  }
  /**
   * Alias for `emitter.removeListener()`.
   *
   * @example
   * emitter.off('hello', listener)
   */
  off(eventName, listener) {
    return this.removeListener(eventName, listener);
  }
  removeAllListeners(eventName) {
    if (eventName) {
      this.events.delete(eventName);
    } else {
      this.events.clear();
    }
    return this;
  }
  /**
   * Returns a copy of the array of listeners for the event named `eventName`.
   */
  listeners(eventName) {
    return Array.from(this._getListeners(eventName));
  }
  /**
   * Returns the number of listeners listening to the event named `eventName`.
   */
  listenerCount(eventName) {
    return this._getListeners(eventName).length;
  }
  rawListeners(eventName) {
    return this.listeners(eventName);
  }
};
var Emitter2 = _Emitter;
Emitter2.defaultMaxListeners = 10;
var INTERNAL_REQUEST_ID_HEADER_NAME = "x-interceptors-internal-request-id";
function getGlobalSymbol(symbol) {
  return globalThis[symbol] || void 0;
}
function setGlobalSymbol(symbol, value) {
  globalThis[symbol] = value;
}
function deleteGlobalSymbol(symbol) {
  delete globalThis[symbol];
}
var InterceptorReadyState = function(InterceptorReadyState$1) {
  InterceptorReadyState$1["INACTIVE"] = "INACTIVE";
  InterceptorReadyState$1["APPLYING"] = "APPLYING";
  InterceptorReadyState$1["APPLIED"] = "APPLIED";
  InterceptorReadyState$1["DISPOSING"] = "DISPOSING";
  InterceptorReadyState$1["DISPOSED"] = "DISPOSED";
  return InterceptorReadyState$1;
}({});
var Interceptor2 = class {
  constructor(symbol) {
    this.symbol = symbol;
    this.readyState = InterceptorReadyState.INACTIVE;
    this.emitter = new Emitter2();
    this.subscriptions = [];
    this.logger = new Logger(symbol.description);
    this.emitter.setMaxListeners(0);
    this.logger.info("constructing the interceptor...");
  }
  /**
  * Determine if this interceptor can be applied
  * in the current environment.
  */
  checkEnvironment() {
    return true;
  }
  /**
  * Apply this interceptor to the current process.
  * Returns an already running interceptor instance if it's present.
  */
  apply() {
    const logger = this.logger.extend("apply");
    logger.info("applying the interceptor...");
    if (this.readyState === InterceptorReadyState.APPLIED) {
      logger.info("intercepted already applied!");
      return;
    }
    if (!this.checkEnvironment()) {
      logger.info("the interceptor cannot be applied in this environment!");
      return;
    }
    this.readyState = InterceptorReadyState.APPLYING;
    const runningInstance = this.getInstance();
    if (runningInstance) {
      logger.info("found a running instance, reusing...");
      this.on = (event, listener) => {
        logger.info('proxying the "%s" listener', event);
        runningInstance.emitter.addListener(event, listener);
        this.subscriptions.push(() => {
          runningInstance.emitter.removeListener(event, listener);
          logger.info('removed proxied "%s" listener!', event);
        });
        return this;
      };
      this.readyState = InterceptorReadyState.APPLIED;
      return;
    }
    logger.info("no running instance found, setting up a new instance...");
    this.setup();
    this.setInstance();
    this.readyState = InterceptorReadyState.APPLIED;
  }
  /**
  * Setup the module augments and stubs necessary for this interceptor.
  * This method is not run if there's a running interceptor instance
  * to prevent instantiating an interceptor multiple times.
  */
  setup() {
  }
  /**
  * Listen to the interceptor's public events.
  */
  on(event, listener) {
    const logger = this.logger.extend("on");
    if (this.readyState === InterceptorReadyState.DISPOSING || this.readyState === InterceptorReadyState.DISPOSED) {
      logger.info("cannot listen to events, already disposed!");
      return this;
    }
    logger.info('adding "%s" event listener:', event, listener);
    this.emitter.on(event, listener);
    return this;
  }
  once(event, listener) {
    this.emitter.once(event, listener);
    return this;
  }
  off(event, listener) {
    this.emitter.off(event, listener);
    return this;
  }
  removeAllListeners(event) {
    this.emitter.removeAllListeners(event);
    return this;
  }
  /**
  * Disposes of any side-effects this interceptor has introduced.
  */
  dispose() {
    const logger = this.logger.extend("dispose");
    if (this.readyState === InterceptorReadyState.DISPOSED) {
      logger.info("cannot dispose, already disposed!");
      return;
    }
    logger.info("disposing the interceptor...");
    this.readyState = InterceptorReadyState.DISPOSING;
    if (!this.getInstance()) {
      logger.info("no interceptors running, skipping dispose...");
      return;
    }
    this.clearInstance();
    logger.info("global symbol deleted:", getGlobalSymbol(this.symbol));
    if (this.subscriptions.length > 0) {
      logger.info("disposing of %d subscriptions...", this.subscriptions.length);
      for (const dispose of this.subscriptions) dispose();
      this.subscriptions = [];
      logger.info("disposed of all subscriptions!", this.subscriptions.length);
    }
    this.emitter.removeAllListeners();
    logger.info("destroyed the listener!");
    this.readyState = InterceptorReadyState.DISPOSED;
  }
  getInstance() {
    var _a20;
    const instance = getGlobalSymbol(this.symbol);
    this.logger.info("retrieved global instance:", (_a20 = instance == null ? void 0 : instance.constructor) == null ? void 0 : _a20.name);
    return instance;
  }
  setInstance() {
    setGlobalSymbol(this.symbol, this);
    this.logger.info("set global instance!", this.symbol.description);
  }
  clearInstance() {
    deleteGlobalSymbol(this.symbol);
    this.logger.info("cleared global instance!", this.symbol.description);
  }
};
function createRequestId2() {
  return Math.random().toString(16).slice(2);
}
function resolveWebSocketUrl2(url) {
  if (typeof url === "string") return resolveWebSocketUrl2(new URL(url, typeof location !== "undefined" ? location.href : void 0));
  if (url.protocol === "http:") url.protocol = "ws:";
  else if (url.protocol === "https:") url.protocol = "wss:";
  if (url.protocol !== "ws:" && url.protocol !== "wss:")
    throw new SyntaxError(`Failed to construct 'WebSocket': The URL's scheme must be either 'http', 'https', 'ws', or 'wss'. '${url.protocol}' is not allowed.`);
  if (url.hash !== "") throw new SyntaxError(`Failed to construct 'WebSocket': The URL contains a fragment identifier ('${url.hash}'). Fragment identifiers are not allowed in WebSocket URLs.`);
  return url.href;
}
async function emitAsync2(emitter, eventName, ...data) {
  const listeners = emitter.listeners(eventName);
  if (listeners.length === 0) return;
  for (const listener of listeners) await listener.apply(emitter, data);
}
function hasConfigurableGlobal2(propertyName) {
  const descriptor = Object.getOwnPropertyDescriptor(globalThis, propertyName);
  if (typeof descriptor === "undefined") return false;
  if (typeof descriptor.get === "function" && typeof descriptor.get() === "undefined") return false;
  if (typeof descriptor.get === "undefined" && descriptor.value == null) return false;
  if (typeof descriptor.set === "undefined" && !descriptor.configurable) {
    console.error(`[MSW] Failed to apply interceptor: the global \`${propertyName}\` property is non-configurable. This is likely an issue with your environment. If you are using a framework, please open an issue about this in their repository.`);
    return false;
  }
  return true;
}
function createDeferredExecutor() {
  const executor = (resolve, reject) => {
    executor.state = "pending";
    executor.resolve = (data) => {
      if (executor.state !== "pending") {
        return;
      }
      executor.result = data;
      const onFulfilled = (value) => {
        executor.state = "fulfilled";
        return value;
      };
      return resolve(
        data instanceof Promise ? data : Promise.resolve(data).then(onFulfilled)
      );
    };
    executor.reject = (reason) => {
      if (executor.state !== "pending") {
        return;
      }
      queueMicrotask(() => {
        executor.state = "rejected";
      });
      return reject(executor.rejectionReason = reason);
    };
  };
  return executor;
}
var _executor, _DeferredPromise_instances, decorate_fn, _a4;
var DeferredPromise2 = (_a4 = class extends Promise {
  constructor(executor = null) {
    const deferredExecutor = createDeferredExecutor();
    super((originalResolve, originalReject) => {
      deferredExecutor(originalResolve, originalReject);
      executor == null ? void 0 : executor(deferredExecutor.resolve, deferredExecutor.reject);
    });
    __privateAdd(this, _DeferredPromise_instances);
    __privateAdd(this, _executor);
    __publicField(this, "resolve");
    __publicField(this, "reject");
    __privateSet(this, _executor, deferredExecutor);
    this.resolve = __privateGet(this, _executor).resolve;
    this.reject = __privateGet(this, _executor).reject;
  }
  get state() {
    return __privateGet(this, _executor).state;
  }
  get rejectionReason() {
    return __privateGet(this, _executor).rejectionReason;
  }
  then(onFulfilled, onRejected) {
    return __privateMethod(this, _DeferredPromise_instances, decorate_fn).call(this, super.then(onFulfilled, onRejected));
  }
  catch(onRejected) {
    return __privateMethod(this, _DeferredPromise_instances, decorate_fn).call(this, super.catch(onRejected));
  }
  finally(onfinally) {
    return __privateMethod(this, _DeferredPromise_instances, decorate_fn).call(this, super.finally(onfinally));
  }
}, _executor = new WeakMap(), _DeferredPromise_instances = new WeakSet(), decorate_fn = function(promise) {
  return Object.defineProperties(promise, {
    resolve: { configurable: true, value: this.resolve },
    reject: { configurable: true, value: this.reject }
  });
}, _a4);
function bindEvent2(target, event) {
  Object.defineProperties(event, {
    target: {
      value: target,
      enumerable: true,
      writable: true
    },
    currentTarget: {
      value: target,
      enumerable: true,
      writable: true
    }
  });
  return event;
}
var kCancelable2 = Symbol("kCancelable");
var kDefaultPrevented2 = Symbol("kDefaultPrevented");
var CancelableMessageEvent2 = class extends MessageEvent {
  constructor(type, init) {
    super(type, init);
    this[kCancelable2] = !!init.cancelable;
    this[kDefaultPrevented2] = false;
  }
  get cancelable() {
    return this[kCancelable2];
  }
  set cancelable(nextCancelable) {
    this[kCancelable2] = nextCancelable;
  }
  get defaultPrevented() {
    return this[kDefaultPrevented2];
  }
  set defaultPrevented(nextDefaultPrevented) {
    this[kDefaultPrevented2] = nextDefaultPrevented;
  }
  preventDefault() {
    if (this.cancelable && !this[kDefaultPrevented2]) this[kDefaultPrevented2] = true;
  }
};
var CloseEvent2 = class extends Event {
  constructor(type, init = {}) {
    super(type, init);
    this.code = init.code === void 0 ? 0 : init.code;
    this.reason = init.reason === void 0 ? "" : init.reason;
    this.wasClean = init.wasClean === void 0 ? false : init.wasClean;
  }
};
var CancelableCloseEvent2 = class extends CloseEvent2 {
  constructor(type, init = {}) {
    super(type, init);
    this[kCancelable2] = !!init.cancelable;
    this[kDefaultPrevented2] = false;
  }
  get cancelable() {
    return this[kCancelable2];
  }
  set cancelable(nextCancelable) {
    this[kCancelable2] = nextCancelable;
  }
  get defaultPrevented() {
    return this[kDefaultPrevented2];
  }
  set defaultPrevented(nextDefaultPrevented) {
    this[kDefaultPrevented2] = nextDefaultPrevented;
  }
  preventDefault() {
    if (this.cancelable && !this[kDefaultPrevented2]) this[kDefaultPrevented2] = true;
  }
};
var kEmitter$12 = Symbol("kEmitter");
var kBoundListener$12 = Symbol("kBoundListener");
var WebSocketClientConnection2 = class {
  constructor(socket, transport) {
    this.socket = socket;
    this.transport = transport;
    this.id = createRequestId2();
    this.url = new URL(socket.url);
    this[kEmitter$12] = new EventTarget();
    this.transport.addEventListener("outgoing", (event) => {
      const message = bindEvent2(this.socket, new CancelableMessageEvent2("message", {
        data: event.data,
        origin: event.origin,
        cancelable: true
      }));
      this[kEmitter$12].dispatchEvent(message);
      if (message.defaultPrevented) event.preventDefault();
    });
    this.transport.addEventListener("close", (event) => {
      this[kEmitter$12].dispatchEvent(bindEvent2(this.socket, new CloseEvent2("close", event)));
    });
  }
  /**
  * Listen for the outgoing events from the connected WebSocket client.
  */
  addEventListener(type, listener, options) {
    if (!Reflect.has(listener, kBoundListener$12)) {
      const boundListener = listener.bind(this.socket);
      Object.defineProperty(listener, kBoundListener$12, {
        value: boundListener,
        enumerable: false,
        configurable: false
      });
    }
    this[kEmitter$12].addEventListener(type, Reflect.get(listener, kBoundListener$12), options);
  }
  /**
  * Removes the listener for the given event.
  */
  removeEventListener(event, listener, options) {
    this[kEmitter$12].removeEventListener(event, Reflect.get(listener, kBoundListener$12), options);
  }
  /**
  * Send data to the connected client.
  */
  send(data) {
    this.transport.send(data);
  }
  /**
  * Close the WebSocket connection.
  * @param {number} code A status code (see https://www.rfc-editor.org/rfc/rfc6455#section-7.4.1).
  * @param {string} reason A custom connection close reason.
  */
  close(code, reason) {
    this.transport.close(code, reason);
  }
};
var WEBSOCKET_CLOSE_CODE_RANGE_ERROR2 = "InvalidAccessError: close code out of user configurable range";
var kPassthroughPromise2 = Symbol("kPassthroughPromise");
var kOnSend2 = Symbol("kOnSend");
var kClose2 = Symbol("kClose");
var _a5;
var WebSocketOverride2 = (_a5 = class extends EventTarget {
  constructor(url, protocols) {
    super();
    this.CONNECTING = 0;
    this.OPEN = 1;
    this.CLOSING = 2;
    this.CLOSED = 3;
    this._onopen = null;
    this._onmessage = null;
    this._onerror = null;
    this._onclose = null;
    this.url = resolveWebSocketUrl2(url);
    this.protocol = "";
    this.extensions = "";
    this.binaryType = "blob";
    this.readyState = this.CONNECTING;
    this.bufferedAmount = 0;
    this[kPassthroughPromise2] = new DeferredPromise2();
    queueMicrotask(async () => {
      if (await this[kPassthroughPromise2]) return;
      this.protocol = typeof protocols === "string" ? protocols : Array.isArray(protocols) && protocols.length > 0 ? protocols[0] : "";
      if (this.readyState === this.CONNECTING) {
        this.readyState = this.OPEN;
        this.dispatchEvent(bindEvent2(this, new Event("open")));
      }
    });
  }
  set onopen(listener) {
    this.removeEventListener("open", this._onopen);
    this._onopen = listener;
    if (listener !== null) this.addEventListener("open", listener);
  }
  get onopen() {
    return this._onopen;
  }
  set onmessage(listener) {
    this.removeEventListener("message", this._onmessage);
    this._onmessage = listener;
    if (listener !== null) this.addEventListener("message", listener);
  }
  get onmessage() {
    return this._onmessage;
  }
  set onerror(listener) {
    this.removeEventListener("error", this._onerror);
    this._onerror = listener;
    if (listener !== null) this.addEventListener("error", listener);
  }
  get onerror() {
    return this._onerror;
  }
  set onclose(listener) {
    this.removeEventListener("close", this._onclose);
    this._onclose = listener;
    if (listener !== null) this.addEventListener("close", listener);
  }
  get onclose() {
    return this._onclose;
  }
  /**
  * @see https://websockets.spec.whatwg.org/#ref-for-dom-websocket-send%E2%91%A0
  */
  send(data) {
    if (this.readyState === this.CONNECTING) {
      this.close();
      throw new DOMException("InvalidStateError");
    }
    if (this.readyState === this.CLOSING || this.readyState === this.CLOSED) return;
    this.bufferedAmount += getDataSize2(data);
    queueMicrotask(() => {
      var _a20;
      this.bufferedAmount = 0;
      (_a20 = this[kOnSend2]) == null ? void 0 : _a20.call(this, data);
    });
  }
  close(code = 1e3, reason) {
    invariant2(code, WEBSOCKET_CLOSE_CODE_RANGE_ERROR2);
    invariant2(code === 1e3 || code >= 3e3 && code <= 4999, WEBSOCKET_CLOSE_CODE_RANGE_ERROR2);
    this[kClose2](code, reason);
  }
  [kClose2](code = 1e3, reason, wasClean = true) {
    if (this.readyState === this.CLOSING || this.readyState === this.CLOSED) return;
    this.readyState = this.CLOSING;
    queueMicrotask(() => {
      this.readyState = this.CLOSED;
      this.dispatchEvent(bindEvent2(this, new CloseEvent2("close", {
        code,
        reason,
        wasClean
      })));
      this._onopen = null;
      this._onmessage = null;
      this._onerror = null;
      this._onclose = null;
    });
  }
  addEventListener(type, listener, options) {
    return super.addEventListener(type, listener, options);
  }
  removeEventListener(type, callback, options) {
    return super.removeEventListener(type, callback, options);
  }
}, _a5.CONNECTING = 0, _a5.OPEN = 1, _a5.CLOSING = 2, _a5.CLOSED = 3, _a5);
function getDataSize2(data) {
  if (typeof data === "string") return data.length;
  if (data instanceof Blob) return data.size;
  return data.byteLength;
}
var kEmitter2 = Symbol("kEmitter");
var kBoundListener2 = Symbol("kBoundListener");
var kSend2 = Symbol("kSend");
var WebSocketServerConnection2 = class {
  constructor(client, transport, createConnection) {
    this.client = client;
    this.transport = transport;
    this.createConnection = createConnection;
    this[kEmitter2] = new EventTarget();
    this.mockCloseController = new AbortController();
    this.realCloseController = new AbortController();
    this.transport.addEventListener("outgoing", (event) => {
      if (typeof this.realWebSocket === "undefined") return;
      queueMicrotask(() => {
        if (!event.defaultPrevented)
          this[kSend2](event.data);
      });
    });
    this.transport.addEventListener("incoming", this.handleIncomingMessage.bind(this));
  }
  /**
  * The `WebSocket` instance connected to the original server.
  * Accessing this before calling `server.connect()` will throw.
  */
  get socket() {
    invariant2(this.realWebSocket, 'Cannot access "socket" on the original WebSocket server object: the connection is not open. Did you forget to call `server.connect()`?');
    return this.realWebSocket;
  }
  /**
  * Open connection to the original WebSocket server.
  */
  connect() {
    invariant2(!this.realWebSocket || this.realWebSocket.readyState !== WebSocket.OPEN, 'Failed to call "connect()" on the original WebSocket instance: the connection already open');
    const realWebSocket = this.createConnection();
    realWebSocket.binaryType = this.client.binaryType;
    realWebSocket.addEventListener("open", (event) => {
      this[kEmitter2].dispatchEvent(bindEvent2(this.realWebSocket, new Event("open", event)));
    }, { once: true });
    realWebSocket.addEventListener("message", (event) => {
      this.transport.dispatchEvent(bindEvent2(this.realWebSocket, new MessageEvent("incoming", {
        data: event.data,
        origin: event.origin
      })));
    });
    this.client.addEventListener("close", (event) => {
      this.handleMockClose(event);
    }, { signal: this.mockCloseController.signal });
    realWebSocket.addEventListener("close", (event) => {
      this.handleRealClose(event);
    }, { signal: this.realCloseController.signal });
    realWebSocket.addEventListener("error", () => {
      const errorEvent = bindEvent2(realWebSocket, new Event("error", { cancelable: true }));
      this[kEmitter2].dispatchEvent(errorEvent);
      if (!errorEvent.defaultPrevented) this.client.dispatchEvent(bindEvent2(this.client, new Event("error")));
    });
    this.realWebSocket = realWebSocket;
  }
  /**
  * Listen for the incoming events from the original WebSocket server.
  */
  addEventListener(event, listener, options) {
    if (!Reflect.has(listener, kBoundListener2)) {
      const boundListener = listener.bind(this.client);
      Object.defineProperty(listener, kBoundListener2, {
        value: boundListener,
        enumerable: false
      });
    }
    this[kEmitter2].addEventListener(event, Reflect.get(listener, kBoundListener2), options);
  }
  /**
  * Remove the listener for the given event.
  */
  removeEventListener(event, listener, options) {
    this[kEmitter2].removeEventListener(event, Reflect.get(listener, kBoundListener2), options);
  }
  /**
  * Send data to the original WebSocket server.
  * @example
  * server.send('hello')
  * server.send(new Blob(['hello']))
  * server.send(new TextEncoder().encode('hello'))
  */
  send(data) {
    this[kSend2](data);
  }
  [kSend2](data) {
    const { realWebSocket } = this;
    invariant2(realWebSocket, 'Failed to call "server.send()" for "%s": the connection is not open. Did you forget to call "server.connect()"?', this.client.url);
    if (realWebSocket.readyState === WebSocket.CLOSING || realWebSocket.readyState === WebSocket.CLOSED) return;
    if (realWebSocket.readyState === WebSocket.CONNECTING) {
      realWebSocket.addEventListener("open", () => {
        realWebSocket.send(data);
      }, { once: true });
      return;
    }
    realWebSocket.send(data);
  }
  /**
  * Close the actual server connection.
  */
  close() {
    const { realWebSocket } = this;
    invariant2(realWebSocket, 'Failed to close server connection for "%s": the connection is not open. Did you forget to call "server.connect()"?', this.client.url);
    this.realCloseController.abort();
    if (realWebSocket.readyState === WebSocket.CLOSING || realWebSocket.readyState === WebSocket.CLOSED) return;
    realWebSocket.close();
    queueMicrotask(() => {
      this[kEmitter2].dispatchEvent(bindEvent2(this.realWebSocket, new CancelableCloseEvent2("close", {
        code: 1e3,
        cancelable: true
      })));
    });
  }
  handleIncomingMessage(event) {
    const messageEvent = bindEvent2(event.target, new CancelableMessageEvent2("message", {
      data: event.data,
      origin: event.origin,
      cancelable: true
    }));
    this[kEmitter2].dispatchEvent(messageEvent);
    if (!messageEvent.defaultPrevented) this.client.dispatchEvent(bindEvent2(
      /**
      * @note Bind the forwarded original server events
      * to the mock WebSocket instance so it would
      * dispatch them straight away.
      */
      this.client,
      new MessageEvent("message", {
        data: event.data,
        origin: event.origin
      })
    ));
  }
  handleMockClose(_event2) {
    if (this.realWebSocket) this.realWebSocket.close();
  }
  handleRealClose(event) {
    this.mockCloseController.abort();
    const closeEvent = bindEvent2(this.realWebSocket, new CancelableCloseEvent2("close", {
      code: event.code,
      reason: event.reason,
      wasClean: event.wasClean,
      cancelable: true
    }));
    this[kEmitter2].dispatchEvent(closeEvent);
    if (!closeEvent.defaultPrevented) this.client[kClose2](event.code, event.reason);
  }
};
var WebSocketClassTransport2 = class extends EventTarget {
  constructor(socket) {
    super();
    this.socket = socket;
    this.socket.addEventListener("close", (event) => {
      this.dispatchEvent(bindEvent2(this.socket, new CloseEvent2("close", event)));
    });
    this.socket[kOnSend2] = (data) => {
      this.dispatchEvent(bindEvent2(this.socket, new CancelableMessageEvent2("outgoing", {
        data,
        origin: this.socket.url,
        cancelable: true
      })));
    };
  }
  addEventListener(type, callback, options) {
    return super.addEventListener(type, callback, options);
  }
  dispatchEvent(event) {
    return super.dispatchEvent(event);
  }
  send(data) {
    queueMicrotask(() => {
      if (this.socket.readyState === this.socket.CLOSING || this.socket.readyState === this.socket.CLOSED) return;
      const dispatchEvent = () => {
        this.socket.dispatchEvent(bindEvent2(
          /**
          * @note Setting this event's "target" to the
          * WebSocket override instance is important.
          * This way it can tell apart original incoming events
          * (must be forwarded to the transport) from the
          * mocked message events like the one below
          * (must be dispatched on the client instance).
          */
          this.socket,
          new MessageEvent("message", {
            data,
            origin: this.socket.url
          })
        ));
      };
      if (this.socket.readyState === this.socket.CONNECTING) this.socket.addEventListener("open", () => {
        dispatchEvent();
      }, { once: true });
      else dispatchEvent();
    });
  }
  close(code, reason) {
    this.socket[kClose2](code, reason);
  }
};
var _a6;
var WebSocketInterceptor2 = (_a6 = class extends Interceptor2 {
  constructor() {
    super(_a6.symbol);
  }
  checkEnvironment() {
    return hasConfigurableGlobal2("WebSocket");
  }
  setup() {
    const originalWebSocketDescriptor = Object.getOwnPropertyDescriptor(globalThis, "WebSocket");
    const WebSocketProxy = new Proxy(globalThis.WebSocket, { construct: (target, args, newTarget) => {
      const [url, protocols] = args;
      const createConnection = () => {
        return Reflect.construct(target, args, newTarget);
      };
      const socket = new WebSocketOverride2(url, protocols);
      const transport = new WebSocketClassTransport2(socket);
      queueMicrotask(async () => {
        try {
          const server = new WebSocketServerConnection2(socket, transport, createConnection);
          const hasConnectionListeners = this.emitter.listenerCount("connection") > 0;
          await emitAsync2(this.emitter, "connection", {
            client: new WebSocketClientConnection2(socket, transport),
            server,
            info: { protocols }
          });
          if (hasConnectionListeners) socket[kPassthroughPromise2].resolve(false);
          else {
            socket[kPassthroughPromise2].resolve(true);
            server.connect();
            server.addEventListener("open", () => {
              socket.dispatchEvent(bindEvent2(socket, new Event("open")));
              if (server["realWebSocket"]) socket.protocol = server["realWebSocket"].protocol;
            });
          }
        } catch (error2) {
          if (error2 instanceof Error) {
            socket.dispatchEvent(new Event("error"));
            if (socket.readyState !== WebSocket.CLOSING && socket.readyState !== WebSocket.CLOSED) socket[kClose2](1011, error2.message, false);
            console.error(error2);
          }
        }
      });
      return socket;
    } });
    Object.defineProperty(globalThis, "WebSocket", {
      value: WebSocketProxy,
      configurable: true
    });
    this.subscriptions.push(() => {
      Object.defineProperty(globalThis, "WebSocket", originalWebSocketDescriptor);
    });
  }
}, _a6.symbol = Symbol("websocket"), _a6);
function supportsServiceWorker() {
  return typeof navigator !== "undefined" && "serviceWorker" in navigator && typeof location !== "undefined" && location.protocol !== "file:";
}
function supportsReadableStreamTransfer() {
  try {
    const stream = new ReadableStream({
      start: (controller) => controller.close()
    });
    const message = new MessageChannel();
    message.port1.postMessage(stream, [stream]);
    return true;
  } catch {
    return false;
  }
}
function createDeferredExecutor2() {
  const executor = (resolve, reject) => {
    executor.state = "pending";
    executor.resolve = (data) => {
      if (executor.state !== "pending") return;
      executor.result = data;
      const onFulfilled = (value) => {
        executor.state = "fulfilled";
        return value;
      };
      return resolve(data instanceof Promise ? data : Promise.resolve(data).then(onFulfilled));
    };
    executor.reject = (reason) => {
      if (executor.state !== "pending") return;
      queueMicrotask(() => {
        executor.state = "rejected";
      });
      return reject(executor.rejectionReason = reason);
    };
  };
  return executor;
}
var _executor2, _DeferredPromise2_instances, decorate_fn2, _a7;
var DeferredPromise22 = (_a7 = class extends Promise {
  constructor(executor = null) {
    const deferredExecutor = createDeferredExecutor2();
    super((originalResolve, originalReject) => {
      deferredExecutor(originalResolve, originalReject);
      executor == null ? void 0 : executor(deferredExecutor.resolve, deferredExecutor.reject);
    });
    __privateAdd(this, _DeferredPromise2_instances);
    __privateAdd(this, _executor2);
    __publicField(this, "resolve");
    __publicField(this, "reject");
    __privateSet(this, _executor2, deferredExecutor);
    this.resolve = __privateGet(this, _executor2).resolve;
    this.reject = __privateGet(this, _executor2).reject;
  }
  get state() {
    return __privateGet(this, _executor2).state;
  }
  get rejectionReason() {
    return __privateGet(this, _executor2).rejectionReason;
  }
  then(onFulfilled, onRejected) {
    return __privateMethod(this, _DeferredPromise2_instances, decorate_fn2).call(this, super.then(onFulfilled, onRejected));
  }
  catch(onRejected) {
    return __privateMethod(this, _DeferredPromise2_instances, decorate_fn2).call(this, super.catch(onRejected));
  }
  finally(onfinally) {
    return __privateMethod(this, _DeferredPromise2_instances, decorate_fn2).call(this, super.finally(onfinally));
  }
}, _executor2 = new WeakMap(), _DeferredPromise2_instances = new WeakSet(), decorate_fn2 = function(promise) {
  return Object.defineProperties(promise, {
    resolve: {
      configurable: true,
      value: this.resolve
    },
    reject: {
      configurable: true,
      value: this.reject
    }
  });
}, _a7);
var IS_PATCHED_MODULE = Symbol("isPatchedModule");
var InterceptorError = class InterceptorError2 extends Error {
  constructor(message) {
    super(message);
    this.name = "InterceptorError";
    Object.setPrototypeOf(this, InterceptorError2.prototype);
  }
};
var _RequestController2_instances, handled_get, _a8;
var RequestController = (_a8 = class {
  constructor(request, source) {
    __privateAdd(this, _RequestController2_instances);
    this.request = request;
    this.source = source;
    this.readyState = _a8.PENDING;
    this.handled = new DeferredPromise2();
  }
  /**
  * Perform this request as-is.
  */
  async passthrough() {
    invariant2.as(InterceptorError, this.readyState === _a8.PENDING, 'Failed to passthrough the "%s %s" request: the request has already been handled', this.request.method, this.request.url);
    this.readyState = _a8.PASSTHROUGH;
    await this.source.passthrough();
    __privateGet(this, _RequestController2_instances, handled_get).resolve();
  }
  /**
  * Respond to this request with the given `Response` instance.
  *
  * @example
  * controller.respondWith(new Response())
  * controller.respondWith(Response.json({ id }))
  * controller.respondWith(Response.error())
  */
  respondWith(response) {
    invariant2.as(InterceptorError, this.readyState === _a8.PENDING, 'Failed to respond to the "%s %s" request with "%d %s": the request has already been handled (%d)', this.request.method, this.request.url, response.status, response.statusText || "OK", this.readyState);
    this.readyState = _a8.RESPONSE;
    __privateGet(this, _RequestController2_instances, handled_get).resolve();
    this.source.respondWith(response);
  }
  /**
  * Error this request with the given reason.
  *
  * @example
  * controller.errorWith()
  * controller.errorWith(new Error('Oops!'))
  * controller.errorWith({ message: 'Oops!'})
  */
  errorWith(reason) {
    invariant2.as(InterceptorError, this.readyState === _a8.PENDING, 'Failed to error the "%s %s" request with "%s": the request has already been handled (%d)', this.request.method, this.request.url, reason == null ? void 0 : reason.toString(), this.readyState);
    this.readyState = _a8.ERROR;
    this.source.errorWith(reason);
    __privateGet(this, _RequestController2_instances, handled_get).resolve();
  }
}, _RequestController2_instances = new WeakSet(), handled_get = function() {
  return this.handled;
}, _a8.PENDING = 0, _a8.PASSTHROUGH = 1, _a8.RESPONSE = 2, _a8.ERROR = 3, _a8);
function canParseUrl(url) {
  try {
    new URL(url);
    return true;
  } catch (_error) {
    return false;
  }
}
function getValueBySymbol(symbolName, source) {
  const symbol = Object.getOwnPropertySymbols(source).find((symbol$1) => {
    return symbol$1.description === symbolName;
  });
  if (symbol) return Reflect.get(source, symbol);
}
var _a9;
var FetchResponse = (_a9 = class extends Response {
  static isConfigurableStatusCode(status) {
    return status >= 200 && status <= 599;
  }
  static isRedirectResponse(status) {
    return _a9.STATUS_CODES_WITH_REDIRECT.includes(status);
  }
  /**
  * Returns a boolean indicating whether the given response status
  * code represents a response that can have a body.
  */
  static isResponseWithBody(status) {
    return !_a9.STATUS_CODES_WITHOUT_BODY.includes(status);
  }
  static setUrl(url, response) {
    if (!url || url === "about:" || !canParseUrl(url)) return;
    const state = getValueBySymbol("state", response);
    if (state) state.urlList.push(new URL(url));
    else Object.defineProperty(response, "url", {
      value: url,
      enumerable: true,
      configurable: true,
      writable: false
    });
  }
  /**
  * Parses the given raw HTTP headers into a Fetch API `Headers` instance.
  */
  static parseRawHeaders(rawHeaders) {
    const headers = new Headers();
    for (let line = 0; line < rawHeaders.length; line += 2) headers.append(rawHeaders[line], rawHeaders[line + 1]);
    return headers;
  }
  constructor(body, init = {}) {
    const status = init.status ?? 200;
    const safeStatus = _a9.isConfigurableStatusCode(status) ? status : 200;
    const finalBody = _a9.isResponseWithBody(status) ? body : null;
    super(finalBody, {
      status: safeStatus,
      statusText: init.statusText,
      headers: init.headers
    });
    if (status !== safeStatus) {
      const state = getValueBySymbol("state", this);
      if (state) state.status = status;
      else Object.defineProperty(this, "status", {
        value: status,
        enumerable: true,
        configurable: true,
        writable: false
      });
    }
    _a9.setUrl(init.url, this);
  }
}, _a9.STATUS_CODES_WITHOUT_BODY = [
  101,
  103,
  204,
  205,
  304
], _a9.STATUS_CODES_WITH_REDIRECT = [
  301,
  302,
  303,
  307,
  308
], _a9);
var kRawRequest = Symbol("kRawRequest");
function setRawRequest(request, rawRequest) {
  Reflect.set(request, kRawRequest, rawRequest);
}
var encoder = new TextEncoder();
function encodeBuffer(text) {
  return encoder.encode(text);
}
function decodeBuffer(buffer, encoding) {
  return new TextDecoder(encoding).decode(buffer);
}
function toArrayBuffer(array) {
  return array.buffer.slice(array.byteOffset, array.byteOffset + array.byteLength);
}
async function until2(callback) {
  try {
    return [null, await callback().catch((error2) => {
      throw error2;
    })];
  } catch (error2) {
    return [error2, null];
  }
}
function getAbsoluteWorkerUrl(workerUrl) {
  return new URL(workerUrl, location.href).href;
}
function getWorkerByRegistration(registration, absoluteWorkerUrl, findWorker) {
  const allStates = [
    registration.active,
    registration.installing,
    registration.waiting
  ];
  const relevantStates = allStates.filter((state) => {
    return state != null;
  });
  const worker = relevantStates.find((worker2) => {
    return findWorker(worker2.scriptURL, absoluteWorkerUrl);
  });
  return worker || null;
}
var getWorkerInstance = async (url, options = {}, findWorker) => {
  const absoluteWorkerUrl = getAbsoluteWorkerUrl(url);
  const mockRegistrations = await navigator.serviceWorker.getRegistrations().then(
    (registrations) => registrations.filter(
      (registration) => getWorkerByRegistration(registration, absoluteWorkerUrl, findWorker)
    )
  );
  if (!navigator.serviceWorker.controller && mockRegistrations.length > 0) {
    location.reload();
  }
  const [existingRegistration] = mockRegistrations;
  if (existingRegistration) {
    existingRegistration.update();
    return [
      getWorkerByRegistration(
        existingRegistration,
        absoluteWorkerUrl,
        findWorker
      ),
      existingRegistration
    ];
  }
  const [registrationError, registrationResult] = await until2(async () => {
    const registration = await navigator.serviceWorker.register(url, options);
    return [
      // Compare existing worker registration by its worker URL,
      // to prevent irrelevant workers to resolve here (such as Codesandbox worker).
      getWorkerByRegistration(registration, absoluteWorkerUrl, findWorker),
      registration
    ];
  });
  if (registrationError) {
    const isWorkerMissing = registrationError.message.includes("(404)");
    if (isWorkerMissing) {
      const scopeUrl = new URL((options == null ? void 0 : options.scope) || "/", location.href);
      throw new Error(
        devUtils.formatMessage(`Failed to register a Service Worker for scope ('${scopeUrl.href}') with script ('${absoluteWorkerUrl}'): Service Worker script does not exist at the given path.

Did you forget to run "npx msw init <PUBLIC_DIR>"?

Learn more about creating the Service Worker script: https://mswjs.io/docs/cli/init`)
      );
    }
    throw new Error(
      devUtils.formatMessage(
        "Failed to register the Service Worker:\n\n%s",
        registrationError.message
      )
    );
  }
  return registrationResult;
};
var _list, _lens, _LensList_instances, openLens_fn, _a10;
var LensList = (_a10 = class {
  constructor() {
    __privateAdd(this, _LensList_instances);
    __privateAdd(this, _list);
    __privateAdd(this, _lens);
    __privateSet(this, _list, []);
    __privateSet(this, _lens, /* @__PURE__ */ new Map());
  }
  get [Symbol.iterator]() {
    return __privateGet(this, _list)[Symbol.iterator].bind(__privateGet(this, _list));
  }
  entries() {
    return __privateGet(this, _lens).entries();
  }
  /**
  * Return an order-sensitive list of values by the given key.
  */
  get(key) {
    return __privateGet(this, _lens).get(key) || [];
  }
  /**
  * Return an order-sensitive list of all values.
  */
  getAll() {
    return __privateGet(this, _list).map(([, value]) => value);
  }
  /**
  * Append a new value to the given key.
  */
  append(key, value) {
    __privateGet(this, _list).push([key, value]);
    __privateMethod(this, _LensList_instances, openLens_fn).call(this, key, (list) => list.push(value));
  }
  /**
  * Prepend a new value to the given key.
  */
  prepend(key, value) {
    __privateGet(this, _list).unshift([key, value]);
    __privateMethod(this, _LensList_instances, openLens_fn).call(this, key, (list) => list.unshift(value));
  }
  /**
  * Delete the value belonging to the given key.
  * Returns `true` if the value was present and removed, `false` otherwise.
  */
  delete(key, value) {
    if (this.size === 0) return false;
    const values = __privateGet(this, _lens).get(key);
    if (!values) return false;
    const index = values.indexOf(value);
    if (index === -1) return false;
    values.splice(index, 1);
    __privateGet(this, _list).splice(__privateGet(this, _list).findIndex((item) => item[0] === key && item[1] === value), 1);
    return true;
  }
  /**
  * Delete all values belogning to the given key.
  */
  deleteAll(key) {
    if (this.size === 0) return;
    __privateSet(this, _list, __privateGet(this, _list).filter((item) => item[0] !== key));
    __privateGet(this, _lens).delete(key);
  }
  get size() {
    return __privateGet(this, _list).length;
  }
  clear() {
    if (this.size === 0) return;
    __privateGet(this, _list).length = 0;
    __privateGet(this, _lens).clear();
  }
}, _list = new WeakMap(), _lens = new WeakMap(), _LensList_instances = new WeakSet(), openLens_fn = function(key, setter) {
  setter(__privateGet(this, _lens).get(key) || __privateGet(this, _lens).set(key, []).get(key));
}, _a10);
var kDefaultPrevented22 = Symbol("kDefaultPrevented");
var kPropagationStopped = Symbol("kPropagationStopped");
var kImmediatePropagationStopped = Symbol("kImmediatePropagationStopped");
var _a11, _b, _c, _d, _returnType, _e;
var TypedEvent2 = (_e = class extends (_d = MessageEvent, _c = kDefaultPrevented22, _b = kPropagationStopped, _a11 = kImmediatePropagationStopped, _d) {
  constructor(...args) {
    super(args[0], args[1]);
    /**
    * @note Keep a placeholder property with the return type
    * because the type must be set somewhere in order to be
    * correctly associated and inferred from the event.
    */
    __privateAdd(this, _returnType);
    __publicField(this, _c);
    __publicField(this, _b);
    __publicField(this, _a11);
    this[kDefaultPrevented22] = false;
  }
  get defaultPrevented() {
    return this[kDefaultPrevented22];
  }
  preventDefault() {
    super.preventDefault();
    this[kDefaultPrevented22] = true;
  }
  stopImmediatePropagation() {
    super.stopImmediatePropagation();
    this[kImmediatePropagationStopped] = true;
  }
}, _returnType = new WeakMap(), _e);
var _listeners, _listenerOptions, _listenerAbortCleanups, _typelessListeners, _hookListeners, _hookListenerOptions, _hookListenerAbortCleanups, _Emitter2_instances, deleteHookListener_fn, deleteListener_fn, addListener_fn, proxyEvent_fn, callListener_fn, matchListeners_fn, isTypelessListener_fn, _a12;
var Emitter22 = (_a12 = class {
  constructor() {
    __privateAdd(this, _Emitter2_instances);
    __privateAdd(this, _listeners);
    __privateAdd(this, _listenerOptions);
    __privateAdd(this, _listenerAbortCleanups);
    __privateAdd(this, _typelessListeners);
    __privateAdd(this, _hookListeners);
    __privateAdd(this, _hookListenerOptions);
    __privateAdd(this, _hookListenerAbortCleanups);
    __publicField(this, "hooks");
    __privateSet(this, _listeners, new LensList());
    __privateSet(this, _listenerOptions, /* @__PURE__ */ new WeakMap());
    __privateSet(this, _listenerAbortCleanups, /* @__PURE__ */ new WeakMap());
    __privateSet(this, _typelessListeners, /* @__PURE__ */ new WeakSet());
    __privateSet(this, _hookListeners, new LensList());
    __privateSet(this, _hookListenerOptions, /* @__PURE__ */ new WeakMap());
    __privateSet(this, _hookListenerAbortCleanups, /* @__PURE__ */ new WeakMap());
    this.hooks = {
      on: (hook, callback, options) => {
        var _a20;
        if ((_a20 = options == null ? void 0 : options.signal) == null ? void 0 : _a20.aborted) return;
        if (options == null ? void 0 : options.once) {
          const original = callback;
          const wrapper = (...args) => {
            __privateMethod(this, _Emitter2_instances, deleteHookListener_fn).call(this, hook, wrapper);
            return original(...args);
          };
          callback = wrapper;
        }
        __privateGet(this, _hookListeners).append(hook, callback);
        if (options) __privateGet(this, _hookListenerOptions).set(callback, options);
        if (options == null ? void 0 : options.signal) {
          const { signal } = options;
          const onAbort = () => {
            __privateMethod(this, _Emitter2_instances, deleteHookListener_fn).call(this, hook, callback);
          };
          signal.addEventListener("abort", onAbort, { once: true });
          __privateGet(this, _hookListenerAbortCleanups).set(callback, () => {
            signal.removeEventListener("abort", onAbort);
          });
        }
      },
      removeListener: (hook, callback) => {
        __privateMethod(this, _Emitter2_instances, deleteHookListener_fn).call(this, hook, callback);
      }
    };
  }
  /**
  * Adds a listener for the given event type.
  */
  on(type, listener, options) {
    __privateMethod(this, _Emitter2_instances, addListener_fn).call(this, type, listener, options);
    return this;
  }
  /**
  * Adds a one-time listener for the given event type.
  */
  once(type, listener, options) {
    return this.on(type, listener, {
      ...options || {},
      once: true
    });
  }
  /**
  * Prepends a listener for the given event type.
  */
  earlyOn(type, listener, options) {
    __privateMethod(this, _Emitter2_instances, addListener_fn).call(this, type, listener, options, "prepend");
    return this;
  }
  /**
  * Prepends a one-time listener for the given event type.
  */
  earlyOnce(type, listener, options) {
    return this.earlyOn(type, listener, {
      ...options || {},
      once: true
    });
  }
  /**
  * Emits the given typed event.
  *
  * @returns {boolean} Returns `true` if the event had any listeners, `false` otherwise.
  */
  emit(event) {
    if (__privateGet(this, _listeners).size === 0) return false;
    const hasListeners = this.listenerCount(event.type) > 0;
    const proxiedEvent = __privateMethod(this, _Emitter2_instances, proxyEvent_fn).call(this, event);
    for (const listener of __privateMethod(this, _Emitter2_instances, matchListeners_fn).call(this, event.type)) {
      if (proxiedEvent.event[kPropagationStopped] != null && proxiedEvent.event[kPropagationStopped] !== this) {
        proxiedEvent.revoke();
        return false;
      }
      if (proxiedEvent.event[kImmediatePropagationStopped]) break;
      __privateMethod(this, _Emitter2_instances, callListener_fn).call(this, proxiedEvent.event, listener);
    }
    proxiedEvent.revoke();
    return hasListeners;
  }
  /**
  * Emits the given typed event and returns a promise that resolves
  * when all the listeners for that event have settled.
  *
  * @returns {Promise<Array<Emitter.ListenerReturnType>>} A promise that resolves
  * with the return values of all listeners.
  */
  async emitAsPromise(event) {
    if (__privateGet(this, _listeners).size === 0) return [];
    const pendingListeners = [];
    const proxiedEvent = __privateMethod(this, _Emitter2_instances, proxyEvent_fn).call(this, event);
    for (const listener of __privateMethod(this, _Emitter2_instances, matchListeners_fn).call(this, event.type)) {
      if (proxiedEvent.event[kPropagationStopped] != null && proxiedEvent.event[kPropagationStopped] !== this) {
        proxiedEvent.revoke();
        return [];
      }
      if (proxiedEvent.event[kImmediatePropagationStopped]) break;
      const returnValue = await Promise.resolve(__privateMethod(this, _Emitter2_instances, callListener_fn).call(this, proxiedEvent.event, listener));
      if (!__privateMethod(this, _Emitter2_instances, isTypelessListener_fn).call(this, listener)) pendingListeners.push(returnValue);
    }
    proxiedEvent.revoke();
    return Promise.allSettled(pendingListeners).then((results) => {
      return results.map((result) => result.status === "fulfilled" ? result.value : result.reason);
    });
  }
  /**
  * Emits the given event and returns a generator that yields
  * the result of each listener in the order of their registration.
  * This way, you stop exhausting the listeners once you get the expected value.
  */
  *emitAsGenerator(event) {
    if (__privateGet(this, _listeners).size === 0) return;
    const proxiedEvent = __privateMethod(this, _Emitter2_instances, proxyEvent_fn).call(this, event);
    for (const listener of __privateMethod(this, _Emitter2_instances, matchListeners_fn).call(this, event.type)) {
      if (proxiedEvent.event[kPropagationStopped] != null && proxiedEvent.event[kPropagationStopped] !== this) {
        proxiedEvent.revoke();
        return;
      }
      if (proxiedEvent.event[kImmediatePropagationStopped]) break;
      const returnValue = __privateMethod(this, _Emitter2_instances, callListener_fn).call(this, proxiedEvent.event, listener);
      if (!__privateMethod(this, _Emitter2_instances, isTypelessListener_fn).call(this, listener)) yield returnValue;
    }
    proxiedEvent.revoke();
  }
  /**
  * Removes a listener for the given event type.
  */
  removeListener(type, listener) {
    const options = __privateGet(this, _listenerOptions).get(listener);
    if (!__privateMethod(this, _Emitter2_instances, deleteListener_fn).call(this, type, listener)) return;
    for (const hook of __privateGet(this, _hookListeners).get("removeListener").slice()) hook(type, listener, options);
  }
  /**
  * Removes all listeners for the given event type.
  * If no event type is provided, removes all existing listeners.
  */
  removeAllListeners(type) {
    var _a20;
    if (type == null) {
      for (const [listenerType, listeners$1] of __privateGet(this, _listeners).entries()) while (listeners$1.length > 0) this.removeListener(listenerType, listeners$1[0]);
      for (const [hookType, hookListener] of [...__privateGet(this, _hookListeners)]) if (!((_a20 = __privateGet(this, _hookListenerOptions).get(hookListener)) == null ? void 0 : _a20.persist)) __privateMethod(this, _Emitter2_instances, deleteHookListener_fn).call(this, hookType, hookListener);
      return;
    }
    const listeners = this.listeners(type);
    while (listeners.length > 0) this.removeListener(type, listeners[0]);
  }
  /**
  * Returns the list of listeners for the given event type.
  * If no even type is provided, returns all listeners.
  */
  listeners(type) {
    if (type == null) return __privateGet(this, _listeners).getAll();
    return __privateGet(this, _listeners).get(type);
  }
  /**
  * Returns the number of listeners for the given event type.
  * If no even type is provided, returns the total number of listeners.
  */
  listenerCount(type) {
    if (type == null) return __privateGet(this, _listeners).size;
    return this.listeners(type).length;
  }
}, _listeners = new WeakMap(), _listenerOptions = new WeakMap(), _listenerAbortCleanups = new WeakMap(), _typelessListeners = new WeakMap(), _hookListeners = new WeakMap(), _hookListenerOptions = new WeakMap(), _hookListenerAbortCleanups = new WeakMap(), _Emitter2_instances = new WeakSet(), deleteHookListener_fn = function(hook, callback) {
  __privateGet(this, _hookListeners).delete(hook, callback);
  const cleanup = __privateGet(this, _hookListenerAbortCleanups).get(callback);
  if (cleanup) {
    cleanup();
    __privateGet(this, _hookListenerAbortCleanups).delete(callback);
  }
}, deleteListener_fn = function(type, listener) {
  const removed = __privateGet(this, _listeners).delete(type, listener);
  const cleanup = __privateGet(this, _listenerAbortCleanups).get(listener);
  if (cleanup) {
    cleanup();
    __privateGet(this, _listenerAbortCleanups).delete(listener);
  }
  return removed;
}, addListener_fn = function(type, listener, options, insertMode = "append") {
  var _a20;
  if ((_a20 = options == null ? void 0 : options.signal) == null ? void 0 : _a20.aborted) return;
  for (const hook of __privateGet(this, _hookListeners).get("newListener").slice()) hook(type, listener, options);
  if (type === "*") __privateGet(this, _typelessListeners).add(listener);
  if (insertMode === "prepend") __privateGet(this, _listeners).prepend(type, listener);
  else __privateGet(this, _listeners).append(type, listener);
  if (options) {
    __privateGet(this, _listenerOptions).set(listener, options);
    if (options.signal) {
      const { signal } = options;
      const onAbort = () => {
        this.removeListener(type, listener);
      };
      signal.addEventListener("abort", onAbort, { once: true });
      __privateGet(this, _listenerAbortCleanups).set(listener, () => {
        signal.removeEventListener("abort", onAbort);
      });
    }
  }
}, proxyEvent_fn = function(event) {
  const { stopPropagation } = event;
  event.stopPropagation = () => {
    event[kPropagationStopped] = this;
    stopPropagation.call(event);
  };
  return {
    event,
    revoke() {
      event.stopPropagation = stopPropagation;
    }
  };
}, callListener_fn = function(event, listener) {
  for (const hook of __privateGet(this, _hookListeners).get("beforeEmit").slice()) if (hook(event) === false) return;
  const returnValue = listener.call(this, event);
  const options = __privateGet(this, _listenerOptions).get(listener);
  if (options == null ? void 0 : options.once) {
    const type = __privateMethod(this, _Emitter2_instances, isTypelessListener_fn).call(this, listener) ? "*" : event.type;
    if (__privateMethod(this, _Emitter2_instances, deleteListener_fn).call(this, type, listener)) for (const hook of __privateGet(this, _hookListeners).get("removeListener").slice()) hook(type, listener, options);
  }
  return returnValue;
}, matchListeners_fn = function* (type) {
  const snapshot = [];
  for (const [key, listener] of __privateGet(this, _listeners)) if (key === "*" || key === type) snapshot.push(listener);
  yield* snapshot;
}, isTypelessListener_fn = function(listener) {
  return __privateGet(this, _typelessListeners).has(listener);
}, _a12);
var SUPPORTS_SERVICE_WORKER = supportsServiceWorker();
var _workerEvent, _a13;
var WorkerEvent = (_a13 = class extends TypedEvent2 {
  constructor(workerEvent) {
    const type = workerEvent.data.type;
    const data = workerEvent.data.payload;
    super(
      // @ts-expect-error Troublesome `TypedEvent` extension.
      type,
      { data }
    );
    __privateAdd(this, _workerEvent);
    __privateSet(this, _workerEvent, workerEvent);
  }
  get ports() {
    return __privateGet(this, _workerEvent).ports;
  }
  /**
   * Reply directly to this event using its `MessagePort`.
   */
  postMessage(type, ...rest) {
    __privateGet(this, _workerEvent).ports[0].postMessage(
      { type, data: rest[0] },
      { transfer: rest[1] }
    );
  }
}, _workerEvent = new WeakMap(), _a13);
var _getWorker, _controller2, _a14;
var WorkerChannel = (_a14 = class extends Emitter22 {
  constructor(options) {
    super();
    __privateAdd(this, _getWorker);
    __privateAdd(this, _controller2);
    invariant2(
      SUPPORTS_SERVICE_WORKER,
      "Failed to open a WorkerChannel: Service Worker is not supported in this environment."
    );
    __privateSet(this, _getWorker, options.getWorker);
    __privateSet(this, _controller2, new AbortController());
    navigator.serviceWorker.addEventListener(
      "message",
      async (event) => {
        const worker = await __privateGet(this, _getWorker).call(this);
        if (event.source != null && event.source !== worker) {
          return;
        }
        if (event.data && isObject(event.data) && "type" in event.data) {
          this.emit(new WorkerEvent(event));
        }
      },
      {
        signal: __privateGet(this, _controller2).signal
      }
    );
  }
  /**
   * Send data to the Service Worker controlling this client.
   * This triggers the `message` event listener on ServiceWorkerGlobalScope.
   */
  postMessage(type) {
    invariant2(
      SUPPORTS_SERVICE_WORKER,
      "Failed to post message on a WorkerChannel: the Service Worker API is unavailable in this environment. This is likely an issue with MSW. Please report it on GitHub: https://github.com/mswjs/msw/issues"
    );
    __privateGet(this, _getWorker).call(this).then((worker) => {
      worker.postMessage(type);
    });
  }
  /**
   * Terminal teardown. Removes the `navigator.serviceWorker` message listener
   * and all emitter subscriptions. The channel is not usable afterwards.
   */
  terminate() {
    __privateGet(this, _controller2).abort();
    this.removeAllListeners();
  }
}, _getWorker = new WeakMap(), _controller2 = new WeakMap(), _a14);
function pruneGetRequestBody(request) {
  if (["HEAD", "GET"].includes(request.method)) {
    return void 0;
  }
  return request.body;
}
function deserializeRequest(serializedRequest) {
  return new Request(serializedRequest.url, {
    ...serializedRequest,
    body: pruneGetRequestBody(serializedRequest)
  });
}
function validateWorkerScope(registration) {
  if (!location.href.startsWith(registration.scope)) {
    devUtils.warn(
      `Cannot intercept requests on this page because it's outside of the worker's scope ("${registration.scope}"). If you wish to mock API requests on this page, you must resolve this scope issue.

- (Recommended) Register the worker at the root level ("/") of your application.
- Set the "Service-Worker-Allowed" response header to allow out-of-scope workers.`
    );
  }
}
function shouldInvalidateWorker(prevOptions, nextOptions) {
  return prevOptions.findWorker !== nextOptions.findWorker || prevOptions.serviceWorker.url !== nextOptions.serviceWorker.url || JSON.stringify(prevOptions.serviceWorker.options) !== JSON.stringify(nextOptions.serviceWorker.options);
}
var _a15, _current, _options, _frames2, _channel, _listenerController, _clientPromise, _keepAliveInterval, _stoppedAt, __ServiceWorkerSource_instances, startWorker_fn, handleRequest_fn2, handleResponse_fn2, _defaultFindWorker, checkWorkerIntegrity_fn, printStartMessage_fn, printStopMessage_fn;
var ServiceWorkerSource = (_a15 = class extends NetworkSource {
  constructor(options) {
    super();
    __privateAdd(this, __ServiceWorkerSource_instances);
    __privateAdd(this, _options);
    __privateAdd(this, _frames2);
    __privateAdd(this, _channel);
    __privateAdd(this, _listenerController);
    __privateAdd(this, _clientPromise);
    __privateAdd(this, _keepAliveInterval);
    __privateAdd(this, _stoppedAt);
    __publicField(this, "workerPromise");
    __privateAdd(this, _defaultFindWorker, (workerUrl, mockServiceWorkerUrl) => {
      return workerUrl === mockServiceWorkerUrl;
    });
    invariant2(
      supportsServiceWorker(),
      "Failed to use Service Worker as the network source: the Service Worker API is not supported in this environment"
    );
    __privateSet(this, _options, options);
    __privateSet(this, _frames2, /* @__PURE__ */ new Map());
    this.workerPromise = new DeferredPromise22();
    __privateSet(this, _channel, new WorkerChannel({
      getWorker: () => this.workerPromise.then(([worker]) => worker)
    }));
  }
  /**
   * Create a new Service Worker source or reuse an existing one.
   * These sources act as a singleton and only get recreated if the options change.
   */
  static async from(options) {
    if (__privateGet(_a15, _current) == null) {
      __privateSet(_a15, _current, new _a15(options));
    } else if (shouldInvalidateWorker(__privateGet(__privateGet(_a15, _current), _options), options)) {
      await __privateGet(_a15, _current).terminate();
      __privateSet(_a15, _current, new _a15(options));
    }
    return __privateGet(_a15, _current);
  }
  async enable() {
    if (this.workerPromise.state === "fulfilled" && typeof __privateGet(this, _stoppedAt) == "undefined") {
      devUtils.warn(
        'Found a redundant "worker.start()" call. Note that starting the worker while mocking is already enabled will have no effect. Consider removing this "worker.start()" call.'
      );
      return this.workerPromise.then(([, registration2]) => registration2);
    }
    __privateSet(this, _stoppedAt, void 0);
    __privateGet(this, _channel).removeAllListeners();
    __privateGet(this, _frames2).clear();
    __privateSet(this, _listenerController, new AbortController());
    const [worker, registration] = await __privateMethod(this, __ServiceWorkerSource_instances, startWorker_fn).call(this);
    if (worker.state !== "activated") {
      const controller = new AbortController();
      const activationPromise = new DeferredPromise22();
      activationPromise.then(() => controller.abort());
      worker.addEventListener(
        "statechange",
        () => {
          if (worker.state === "activated") {
            activationPromise.resolve();
          }
        },
        {
          signal: controller.signal
        }
      );
      await activationPromise;
    }
    __privateGet(this, _channel).postMessage("MOCK_ACTIVATE");
    const clientConfirmationPromise = new DeferredPromise22();
    __privateSet(this, _clientPromise, clientConfirmationPromise);
    __privateGet(this, _channel).once("MOCKING_ENABLED", (event) => {
      clientConfirmationPromise.resolve(event.data.client);
    });
    await clientConfirmationPromise;
    if (!__privateGet(this, _options).quiet) {
      __privateMethod(this, __ServiceWorkerSource_instances, printStartMessage_fn).call(this);
    }
    return registration;
  }
  disable() {
    var _a20;
    if (typeof __privateGet(this, _stoppedAt) !== "undefined") {
      devUtils.warn(
        `Found a redundant "worker.stop()" call. Notice that stopping the worker after it has already been stopped has no effect. Consider removing this "worker.stop()" call.`
      );
      return;
    }
    __privateSet(this, _stoppedAt, Date.now());
    (_a20 = __privateGet(this, _listenerController)) == null ? void 0 : _a20.abort();
    __privateSet(this, _listenerController, void 0);
    __privateGet(this, _channel).postMessage("CLIENT_CLOSED");
    if (!__privateGet(this, _options).quiet) {
      __privateMethod(this, __ServiceWorkerSource_instances, printStopMessage_fn).call(this);
    }
  }
  /**
   * Terminal teardown. Unregisters the Service Worker, tears down the channel,
   * and clears timers. Called when the singleton is being replaced with one
   * that has different options. The instance is not usable afterwards.
   */
  async terminate() {
    var _a20;
    if (__privateGet(this, _keepAliveInterval) != null) {
      clearInterval(__privateGet(this, _keepAliveInterval));
      __privateSet(this, _keepAliveInterval, void 0);
    }
    __privateGet(this, _frames2).clear();
    __privateGet(this, _channel).terminate();
    (_a20 = __privateGet(this, _listenerController)) == null ? void 0 : _a20.abort();
    __privateSet(this, _listenerController, void 0);
    if (this.workerPromise.state === "fulfilled") {
      const [, registration] = await this.workerPromise;
      await registration.unregister();
    }
    if (__privateGet(_a15, _current) === this) {
      __privateSet(_a15, _current, void 0);
    }
  }
}, _current = new WeakMap(), _options = new WeakMap(), _frames2 = new WeakMap(), _channel = new WeakMap(), _listenerController = new WeakMap(), _clientPromise = new WeakMap(), _keepAliveInterval = new WeakMap(), _stoppedAt = new WeakMap(), __ServiceWorkerSource_instances = new WeakSet(), startWorker_fn = async function() {
  var _a20, _b2;
  if (__privateGet(this, _keepAliveInterval)) {
    clearInterval(__privateGet(this, _keepAliveInterval));
  }
  const workerUrl = __privateGet(this, _options).serviceWorker.url;
  const [worker, registration] = await getWorkerInstance(
    workerUrl,
    __privateGet(this, _options).serviceWorker.options,
    __privateGet(this, _options).findWorker || __privateGet(this, _defaultFindWorker)
  );
  if (worker == null) {
    const missingWorkerMessage = ((_a20 = __privateGet(this, _options)) == null ? void 0 : _a20.findWorker) ? devUtils.formatMessage(
      `Failed to locate the Service Worker registration using a custom "findWorker" predicate.

Please ensure that the custom predicate properly locates the Service Worker registration at "%s".
More details: https://mswjs.io/docs/api/setup-worker/start#findworker
     `,
      workerUrl
    ) : devUtils.formatMessage(
      `Failed to locate the Service Worker registration.

This most likely means that the worker script URL "%s" cannot resolve against the actual public hostname (%s). This may happen if your application runs behind a proxy, or has a dynamic hostname.

Please consider using a custom "serviceWorker.url" option to point to the actual worker script location, or a custom "findWorker" option to resolve the Service Worker registration manually. More details: https://mswjs.io/docs/api/setup-worker/start`,
      workerUrl,
      location.host
    );
    throw new Error(missingWorkerMessage);
  }
  if (this.workerPromise.state === "pending") {
    this.workerPromise.resolve([worker, registration]);
  } else {
    this.workerPromise = new DeferredPromise22((resolve) => {
      resolve([worker, registration]);
    });
  }
  __privateGet(this, _channel).on("REQUEST", __privateMethod(this, __ServiceWorkerSource_instances, handleRequest_fn2).bind(this));
  __privateGet(this, _channel).on("RESPONSE", __privateMethod(this, __ServiceWorkerSource_instances, handleResponse_fn2).bind(this));
  window.addEventListener(
    "beforeunload",
    () => {
      if (worker.state !== "redundant") {
        __privateGet(this, _channel).postMessage("CLIENT_CLOSED");
      }
      clearInterval(__privateGet(this, _keepAliveInterval));
      window.postMessage({ type: "msw/worker:stop" });
    },
    {
      signal: (_b2 = __privateGet(this, _listenerController)) == null ? void 0 : _b2.signal
    }
  );
  await __privateMethod(this, __ServiceWorkerSource_instances, checkWorkerIntegrity_fn).call(this).catch((error2) => {
    devUtils.error(
      "Error while checking the worker script integrity. Please report this on GitHub (https://github.com/mswjs/msw/issues) and include the original error below."
    );
    console.error(error2);
  });
  __privateSet(this, _keepAliveInterval, window.setInterval(() => {
    __privateGet(this, _channel).postMessage("KEEPALIVE_REQUEST");
  }, 5e3));
  if (!__privateGet(this, _options).quiet) {
    validateWorkerScope(registration);
  }
  return [worker, registration];
}, handleRequest_fn2 = async function(event) {
  if (__privateGet(this, _stoppedAt) && event.data.interceptedAt > __privateGet(this, _stoppedAt)) {
    return event.postMessage("PASSTHROUGH");
  }
  const request = deserializeRequest(event.data);
  RequestHandler.cache.set(request, request.clone());
  const frame = new ServiceWorkerHttpNetworkFrame({
    event,
    request
  });
  __privateGet(this, _frames2).set(event.data.id, frame);
  await this.queue(frame);
}, handleResponse_fn2 = async function(event) {
  var _a20;
  const { request, response, isMockedResponse } = event.data;
  const frame = __privateGet(this, _frames2).get(request.id);
  if ((_a20 = response.type) == null ? void 0 : _a20.includes("opaque")) {
    __privateGet(this, _frames2).delete(request.id);
    frame == null ? void 0 : frame.events.removeAllListeners();
    return;
  }
  __privateGet(this, _frames2).delete(request.id);
  if (frame == null) {
    return;
  }
  const fetchRequest = deserializeRequest(request);
  const fetchResponse = response.status === 0 ? Response.error() : new FetchResponse(
    /**
     * Responses may be streams here, but when we create a response object
     * with null-body status codes, like 204, 205, 304 Response will
     * throw when passed a non-null body, so ensure it's null here
     * for those codes
     */
    FetchResponse.isResponseWithBody(response.status) ? response.body : null,
    {
      ...response,
      /**
       * Set response URL if it's not set already.
       * @see https://github.com/mswjs/msw/issues/2030
       * @see https://developer.mozilla.org/en-US/docs/Web/API/Response/url
       */
      url: request.url
    }
  );
  try {
    frame.events.emit(
      new ResponseEvent(
        isMockedResponse ? "response:mocked" : "response:bypass",
        {
          requestId: frame.data.id,
          request: fetchRequest,
          response: fetchResponse,
          isMockedResponse
        }
      )
    );
  } finally {
    frame.events.removeAllListeners();
  }
}, _defaultFindWorker = new WeakMap(), checkWorkerIntegrity_fn = async function() {
  const integrityCheckPromise = new DeferredPromise22();
  __privateGet(this, _channel).postMessage("INTEGRITY_CHECK_REQUEST");
  __privateGet(this, _channel).once("INTEGRITY_CHECK_RESPONSE", (event) => {
    const { checksum, packageVersion } = event.data;
    if (checksum !== "4db4a41e972cec1b64cc569c66952d82") {
      devUtils.warn(
        `The currently registered Service Worker has been generated by a different version of MSW (${packageVersion}) and may not be fully compatible with the installed version.

It's recommended you update your worker script by running this command:

  • npx msw init <PUBLIC_DIR>

You can also automate this process and make the worker script update automatically upon the library installations. Read more: https://mswjs.io/docs/cli/init.`
      );
    }
    integrityCheckPromise.resolve();
  });
  return integrityCheckPromise;
}, printStartMessage_fn = async function() {
  if (this.workerPromise.state === "rejected") {
    return;
  }
  invariant2(
    __privateGet(this, _clientPromise) != null,
    "[ServiceWorkerSource] Failed to print a start message: client confirmation not received"
  );
  const client = await __privateGet(this, _clientPromise);
  const [worker, registration] = await this.workerPromise;
  console.groupCollapsed(
    `%c${devUtils.formatMessage("Mocking enabled.")}`,
    "color:orangered;font-weight:bold;"
  );
  console.log(
    "%cDocumentation: %chttps://mswjs.io/docs",
    "font-weight:bold",
    "font-weight:normal"
  );
  console.log("Found an issue? https://github.com/mswjs/msw/issues");
  console.log("Worker script URL:", worker.scriptURL);
  console.log("Worker scope:", registration.scope);
  if (client) {
    console.log("Client ID: %s (%s)", client.id, client.frameType);
  }
  console.groupEnd();
}, printStopMessage_fn = function() {
  console.log(
    `%c${devUtils.formatMessage("Mocking disabled.")}`,
    "color:orangered;font-weight:bold;"
  );
}, __privateAdd(_a15, _current), _a15);
var _event, _ServiceWorkerHttpNetworkFrame_instances, respondWith_fn, _a16;
var ServiceWorkerHttpNetworkFrame = (_a16 = class extends HttpNetworkFrame {
  constructor(options) {
    super({ request: options.request });
    __privateAdd(this, _ServiceWorkerHttpNetworkFrame_instances);
    __privateAdd(this, _event);
    __privateSet(this, _event, options.event);
  }
  passthrough() {
    __privateGet(this, _event).postMessage("PASSTHROUGH");
  }
  respondWith(response) {
    if (response) {
      __privateMethod(this, _ServiceWorkerHttpNetworkFrame_instances, respondWith_fn).call(this, response);
    }
  }
  errorWith(reason) {
    if (reason instanceof Response) {
      return this.respondWith(reason);
    }
    devUtils.warn(
      `Uncaught exception in the request handler for "%s %s". This exception has been gracefully handled as a 500 response, however, it's strongly recommended to resolve this error, as it indicates a mistake in your code. If you wish to mock an error response, please see this guide: https://mswjs.io/docs/http/mocking-responses/error-responses`,
      this.data.request.method,
      this.data.request.url
    );
    const error2 = reason instanceof Error ? reason : new Error((reason == null ? void 0 : reason.toString()) || "Request failure");
    this.respondWith(
      HttpResponse.json(
        {
          name: error2.name,
          message: error2.message,
          stack: error2.stack
        },
        {
          status: 500,
          statusText: "Request Handler Error"
        }
      )
    );
  }
}, _event = new WeakMap(), _ServiceWorkerHttpNetworkFrame_instances = new WeakSet(), respondWith_fn = async function(response) {
  let responseBody;
  let transfer;
  const responseInit = toResponseInit(response);
  if (supportsReadableStreamTransfer()) {
    responseBody = response.body;
    transfer = response.body == null ? void 0 : [response.body];
  } else {
    responseBody = response.body == null ? null : await response.clone().arrayBuffer();
  }
  __privateGet(this, _event).postMessage(
    "MOCK_RESPONSE",
    {
      ...responseInit,
      body: responseBody
    },
    transfer
  );
}, _a16);
var until22 = async (promise) => {
  try {
    const data = await promise().catch((error2) => {
      throw error2;
    });
    return { error: null, data };
  } catch (error2) {
    return { error: error2, data: null };
  }
};
function isObject2(value, loose = false) {
  return loose ? Object.prototype.toString.call(value).startsWith("[object ") : Object.prototype.toString.call(value) === "[object Object]";
}
function isPropertyAccessible(obj, key) {
  try {
    obj[key];
    return true;
  } catch {
    return false;
  }
}
function createServerErrorResponse(body) {
  return new Response(JSON.stringify(body instanceof Error ? {
    name: body.name,
    message: body.message,
    stack: body.stack
  } : body), {
    status: 500,
    statusText: "Unhandled Exception",
    headers: { "Content-Type": "application/json" }
  });
}
function isResponseError(response) {
  return response != null && response instanceof Response && isPropertyAccessible(response, "type") && response.type === "error";
}
function isResponseLike(value) {
  return isObject2(value, true) && isPropertyAccessible(value, "status") && isPropertyAccessible(value, "statusText") && isPropertyAccessible(value, "bodyUsed");
}
function isNodeLikeError(error2) {
  if (error2 == null) return false;
  if (!(error2 instanceof Error)) return false;
  return "code" in error2 && "errno" in error2;
}
async function handleRequest(options) {
  const handleResponse = async (response) => {
    if (response instanceof Error) {
      await options.controller.errorWith(response);
      return true;
    }
    if (isResponseError(response)) {
      await options.controller.respondWith(response);
      return true;
    }
    if (isResponseLike(response)) {
      await options.controller.respondWith(response);
      return true;
    }
    if (isObject2(response)) {
      await options.controller.errorWith(response);
      return true;
    }
    return false;
  };
  const handleResponseError = async (error2) => {
    if (error2 instanceof InterceptorError) throw result.error;
    if (isNodeLikeError(error2)) {
      await options.controller.errorWith(error2);
      return true;
    }
    if (error2 instanceof Response) return await handleResponse(error2);
    return false;
  };
  const requestAbortPromise = new DeferredPromise2();
  if (options.request.signal) {
    if (options.request.signal.aborted) {
      await options.controller.errorWith(options.request.signal.reason);
      return;
    }
    options.request.signal.addEventListener("abort", () => {
      requestAbortPromise.reject(options.request.signal.reason);
    }, { once: true });
  }
  const result = await until22(async () => {
    const requestListenersPromise = emitAsync2(options.emitter, "request", {
      requestId: options.requestId,
      request: options.request,
      controller: options.controller
    });
    await Promise.race([
      requestAbortPromise,
      requestListenersPromise,
      options.controller.handled
    ]);
  });
  if (requestAbortPromise.state === "rejected") {
    await options.controller.errorWith(requestAbortPromise.rejectionReason);
    return;
  }
  if (result.error) {
    if (await handleResponseError(result.error)) return;
    if (options.emitter.listenerCount("unhandledException") > 0) {
      const unhandledExceptionController = new RequestController(options.request, {
        passthrough() {
        },
        async respondWith(response) {
          await handleResponse(response);
        },
        async errorWith(reason) {
          await options.controller.errorWith(reason);
        }
      });
      await emitAsync2(options.emitter, "unhandledException", {
        error: result.error,
        request: options.request,
        requestId: options.requestId,
        controller: unhandledExceptionController
      });
      if (unhandledExceptionController.readyState !== RequestController.PENDING) return;
    }
    await options.controller.respondWith(createServerErrorResponse(result.error));
    return;
  }
  if (options.controller.readyState === RequestController.PENDING) return await options.controller.passthrough();
  return options.controller.handled;
}
function createNetworkError(cause) {
  return Object.assign(new TypeError("Failed to fetch"), { cause });
}
var REQUEST_BODY_HEADERS = [
  "content-encoding",
  "content-language",
  "content-location",
  "content-type",
  "content-length"
];
var kRedirectCount = Symbol("kRedirectCount");
async function followFetchRedirect(request, response) {
  if (response.status !== 303 && request.body != null) return Promise.reject(createNetworkError());
  const requestUrl = new URL(request.url);
  let locationUrl;
  try {
    locationUrl = new URL(response.headers.get("location"), request.url);
  } catch (error2) {
    return Promise.reject(createNetworkError(error2));
  }
  if (!(locationUrl.protocol === "http:" || locationUrl.protocol === "https:")) return Promise.reject(createNetworkError("URL scheme must be a HTTP(S) scheme"));
  if (Reflect.get(request, kRedirectCount) > 20) return Promise.reject(createNetworkError("redirect count exceeded"));
  Object.defineProperty(request, kRedirectCount, { value: (Reflect.get(request, kRedirectCount) || 0) + 1 });
  if (request.mode === "cors" && (locationUrl.username || locationUrl.password) && !sameOrigin(requestUrl, locationUrl)) return Promise.reject(createNetworkError('cross origin not allowed for request mode "cors"'));
  const requestInit = {};
  if ([301, 302].includes(response.status) && request.method === "POST" || response.status === 303 && !["HEAD", "GET"].includes(request.method)) {
    requestInit.method = "GET";
    requestInit.body = null;
    REQUEST_BODY_HEADERS.forEach((headerName) => {
      request.headers.delete(headerName);
    });
  }
  if (!sameOrigin(requestUrl, locationUrl)) {
    request.headers.delete("authorization");
    request.headers.delete("proxy-authorization");
    request.headers.delete("cookie");
    request.headers.delete("host");
  }
  requestInit.headers = request.headers;
  const finalResponse = await fetch(new Request(locationUrl, requestInit));
  Object.defineProperty(finalResponse, "redirected", {
    value: true,
    configurable: true
  });
  return finalResponse;
}
function sameOrigin(left, right) {
  if (left.origin === right.origin && left.origin === "null") return true;
  if (left.protocol === right.protocol && left.hostname === right.hostname && left.port === right.port) return true;
  return false;
}
var BrotliDecompressionStream = class extends TransformStream {
  constructor() {
    console.warn("[Interceptors]: Brotli decompression of response streams is not supported in the browser");
    super({ transform(chunk, controller) {
      controller.enqueue(chunk);
    } });
  }
};
var PipelineStream = class extends TransformStream {
  constructor(transformStreams, ...strategies) {
    super({}, ...strategies);
    const readable = [super.readable, ...transformStreams].reduce((readable$1, transform) => readable$1.pipeThrough(transform));
    Object.defineProperty(this, "readable", { get() {
      return readable;
    } });
  }
};
function parseContentEncoding(contentEncoding) {
  return contentEncoding.toLowerCase().split(",").map((coding) => coding.trim());
}
function createDecompressionStream(contentEncoding) {
  if (contentEncoding === "") return null;
  const codings = parseContentEncoding(contentEncoding);
  if (codings.length === 0) return null;
  return new PipelineStream(codings.reduceRight((transformers, coding) => {
    if (coding === "gzip" || coding === "x-gzip") return transformers.concat(new DecompressionStream("gzip"));
    else if (coding === "deflate") return transformers.concat(new DecompressionStream("deflate"));
    else if (coding === "br") return transformers.concat(new BrotliDecompressionStream());
    else transformers.length = 0;
    return transformers;
  }, []));
}
function decompressResponse(response) {
  if (response.body === null) return null;
  const decompressionStream = createDecompressionStream(response.headers.get("content-encoding") || "");
  if (!decompressionStream) return null;
  response.body.pipeTo(decompressionStream.writable);
  return decompressionStream.readable;
}
var _a17;
var FetchInterceptor = (_a17 = class extends Interceptor2 {
  constructor() {
    super(_a17.symbol);
  }
  checkEnvironment() {
    return hasConfigurableGlobal2("fetch");
  }
  async setup() {
    const pureFetch = globalThis.fetch;
    invariant2(!pureFetch[IS_PATCHED_MODULE], 'Failed to patch the "fetch" module: already patched.');
    globalThis.fetch = async (input, init) => {
      const requestId = createRequestId2();
      const resolvedInput = typeof input === "string" && typeof location !== "undefined" && !canParseUrl(input) ? new URL(input, location.href) : input;
      const request = new Request(resolvedInput, init);
      if (input instanceof Request) setRawRequest(request, input);
      const responsePromise = new DeferredPromise2();
      const controller = new RequestController(request, {
        passthrough: async () => {
          this.logger.info("request has not been handled, passthrough...");
          const requestCloneForResponseEvent = request.clone();
          const { error: responseError, data: originalResponse } = await until22(() => pureFetch(request));
          if (responseError) return responsePromise.reject(responseError);
          this.logger.info("original fetch performed", originalResponse);
          if (this.emitter.listenerCount("response") > 0) {
            this.logger.info('emitting the "response" event...');
            const responseClone = originalResponse.clone();
            await emitAsync2(this.emitter, "response", {
              response: responseClone,
              isMockedResponse: false,
              request: requestCloneForResponseEvent,
              requestId
            });
          }
          responsePromise.resolve(originalResponse);
        },
        respondWith: async (rawResponse) => {
          if (isResponseError(rawResponse)) {
            this.logger.info("request has errored!", { response: rawResponse });
            responsePromise.reject(createNetworkError(rawResponse));
            return;
          }
          this.logger.info("received mocked response!", { rawResponse });
          const decompressedStream = decompressResponse(rawResponse);
          const response = decompressedStream === null ? rawResponse : new FetchResponse(decompressedStream, rawResponse);
          FetchResponse.setUrl(request.url, response);
          if (FetchResponse.isRedirectResponse(response.status)) {
            if (request.redirect === "error") {
              responsePromise.reject(createNetworkError("unexpected redirect"));
              return;
            }
            if (request.redirect === "follow") {
              followFetchRedirect(request, response).then((response$1) => {
                responsePromise.resolve(response$1);
              }, (reason) => {
                responsePromise.reject(reason);
              });
              return;
            }
          }
          if (this.emitter.listenerCount("response") > 0) {
            this.logger.info('emitting the "response" event...');
            await emitAsync2(this.emitter, "response", {
              response: response.clone(),
              isMockedResponse: true,
              request,
              requestId
            });
          }
          responsePromise.resolve(response);
        },
        errorWith: (reason) => {
          this.logger.info("request has been aborted!", { reason });
          responsePromise.reject(reason);
        }
      });
      this.logger.info("[%s] %s", request.method, request.url);
      this.logger.info("awaiting for the mocked response...");
      this.logger.info('emitting the "request" event for %s listener(s)...', this.emitter.listenerCount("request"));
      await handleRequest({
        request,
        requestId,
        emitter: this.emitter,
        controller
      });
      return responsePromise;
    };
    Object.defineProperty(globalThis.fetch, IS_PATCHED_MODULE, {
      enumerable: true,
      configurable: true,
      value: true
    });
    this.subscriptions.push(() => {
      Object.defineProperty(globalThis.fetch, IS_PATCHED_MODULE, { value: void 0 });
      globalThis.fetch = pureFetch;
      this.logger.info('restored native "globalThis.fetch"!', globalThis.fetch.name);
    });
  }
}, _a17.symbol = Symbol("fetch"), _a17);
function concatArrayBuffer(left, right) {
  const result = new Uint8Array(left.byteLength + right.byteLength);
  result.set(left, 0);
  result.set(right, left.byteLength);
  return result;
}
var EventPolyfill = class {
  constructor(type, options) {
    this.NONE = 0;
    this.CAPTURING_PHASE = 1;
    this.AT_TARGET = 2;
    this.BUBBLING_PHASE = 3;
    this.type = "";
    this.srcElement = null;
    this.currentTarget = null;
    this.eventPhase = 0;
    this.isTrusted = true;
    this.composed = false;
    this.cancelable = true;
    this.defaultPrevented = false;
    this.bubbles = true;
    this.lengthComputable = true;
    this.loaded = 0;
    this.total = 0;
    this.cancelBubble = false;
    this.returnValue = true;
    this.type = type;
    this.target = (options == null ? void 0 : options.target) || null;
    this.currentTarget = (options == null ? void 0 : options.currentTarget) || null;
    this.timeStamp = Date.now();
  }
  composedPath() {
    return [];
  }
  initEvent(type, bubbles, cancelable) {
    this.type = type;
    this.bubbles = !!bubbles;
    this.cancelable = !!cancelable;
  }
  preventDefault() {
    this.defaultPrevented = true;
  }
  stopPropagation() {
  }
  stopImmediatePropagation() {
  }
};
var ProgressEventPolyfill = class extends EventPolyfill {
  constructor(type, init) {
    super(type);
    this.lengthComputable = (init == null ? void 0 : init.lengthComputable) || false;
    this.composed = (init == null ? void 0 : init.composed) || false;
    this.loaded = (init == null ? void 0 : init.loaded) || 0;
    this.total = (init == null ? void 0 : init.total) || 0;
  }
};
var SUPPORTS_PROGRESS_EVENT = typeof ProgressEvent !== "undefined";
function createEvent(target, type, init) {
  const progressEvents = [
    "error",
    "progress",
    "loadstart",
    "loadend",
    "load",
    "timeout",
    "abort"
  ];
  const ProgressEventClass = SUPPORTS_PROGRESS_EVENT ? ProgressEvent : ProgressEventPolyfill;
  return progressEvents.includes(type) ? new ProgressEventClass(type, {
    lengthComputable: true,
    loaded: (init == null ? void 0 : init.loaded) || 0,
    total: (init == null ? void 0 : init.total) || 0
  }) : new EventPolyfill(type, {
    target,
    currentTarget: target
  });
}
function findPropertySource(target, propertyName) {
  if (!(propertyName in target)) return null;
  if (Object.prototype.hasOwnProperty.call(target, propertyName)) return target;
  const prototype = Reflect.getPrototypeOf(target);
  return prototype ? findPropertySource(prototype, propertyName) : null;
}
function createProxy(target, options) {
  return new Proxy(target, optionsToProxyHandler(options));
}
function optionsToProxyHandler(options) {
  const { constructorCall, methodCall, getProperty, setProperty } = options;
  const handler = {};
  if (typeof constructorCall !== "undefined") handler.construct = function(target, args, newTarget) {
    const next = Reflect.construct.bind(null, target, args, newTarget);
    return constructorCall.call(newTarget, args, next);
  };
  handler.set = function(target, propertyName, nextValue) {
    const next = () => {
      const propertySource = findPropertySource(target, propertyName) || target;
      const ownDescriptors = Reflect.getOwnPropertyDescriptor(propertySource, propertyName);
      if (typeof (ownDescriptors == null ? void 0 : ownDescriptors.set) !== "undefined") {
        ownDescriptors.set.apply(target, [nextValue]);
        return true;
      }
      return Reflect.defineProperty(propertySource, propertyName, {
        writable: true,
        enumerable: true,
        configurable: true,
        value: nextValue
      });
    };
    if (typeof setProperty !== "undefined") return setProperty.call(target, [propertyName, nextValue], next);
    return next();
  };
  handler.get = function(target, propertyName, receiver) {
    const next = () => target[propertyName];
    const value = typeof getProperty !== "undefined" ? getProperty.call(target, [propertyName, receiver], next) : next();
    if (typeof value === "function") return (...args) => {
      const next$1 = value.bind(target, ...args);
      if (typeof methodCall !== "undefined") return methodCall.call(target, [propertyName, args], next$1);
      return next$1();
    };
    return value;
  };
  return handler;
}
function isDomParserSupportedType(type) {
  return [
    "application/xhtml+xml",
    "application/xml",
    "image/svg+xml",
    "text/html",
    "text/xml"
  ].some((supportedType) => {
    return type.startsWith(supportedType);
  });
}
function parseJson(data) {
  try {
    return JSON.parse(data);
  } catch (_) {
    return null;
  }
}
function createResponse(request, body) {
  return new FetchResponse(FetchResponse.isResponseWithBody(request.status) ? body : null, {
    url: request.responseURL,
    status: request.status,
    statusText: request.statusText,
    headers: createHeadersFromXMLHttpRequestHeaders(request.getAllResponseHeaders())
  });
}
function createHeadersFromXMLHttpRequestHeaders(headersString) {
  const headers = new Headers();
  const lines = headersString.split(/[\r\n]+/);
  for (const line of lines) {
    if (line.trim() === "") continue;
    const [name, ...parts] = line.split(": ");
    const value = parts.join(": ");
    headers.append(name, value);
  }
  return headers;
}
async function getBodyByteLength(input) {
  const explicitContentLength = input.headers.get("content-length");
  if (explicitContentLength != null && explicitContentLength !== "") return Number(explicitContentLength);
  return (await input.arrayBuffer()).byteLength;
}
var kIsRequestHandled = Symbol("kIsRequestHandled");
var IS_NODE2 = isNodeProcess();
var kFetchRequest = Symbol("kFetchRequest");
var XMLHttpRequestController = class {
  constructor(initialRequest, logger) {
    this.initialRequest = initialRequest;
    this.logger = logger;
    this.method = "GET";
    this.url = null;
    this[kIsRequestHandled] = false;
    this.events = /* @__PURE__ */ new Map();
    this.uploadEvents = /* @__PURE__ */ new Map();
    this.requestId = createRequestId2();
    this.requestHeaders = new Headers();
    this.responseBuffer = new Uint8Array();
    this.request = createProxy(initialRequest, {
      setProperty: ([propertyName, nextValue], invoke) => {
        switch (propertyName) {
          case "ontimeout": {
            const eventName = propertyName.slice(2);
            this.request.addEventListener(eventName, nextValue);
            return invoke();
          }
          default:
            return invoke();
        }
      },
      methodCall: ([methodName, args], invoke) => {
        switch (methodName) {
          case "open": {
            const [method, url] = args;
            if (typeof url === "undefined") {
              this.method = "GET";
              this.url = toAbsoluteUrl(method);
            } else {
              this.method = method;
              this.url = toAbsoluteUrl(url);
            }
            this.logger = this.logger.extend(`${this.method} ${this.url.href}`);
            this.logger.info("open", this.method, this.url.href);
            return invoke();
          }
          case "addEventListener": {
            const [eventName, listener] = args;
            this.registerEvent(eventName, listener);
            this.logger.info("addEventListener", eventName, listener);
            return invoke();
          }
          case "setRequestHeader": {
            const [name, value] = args;
            this.requestHeaders.set(name, value);
            this.logger.info("setRequestHeader", name, value);
            return invoke();
          }
          case "send": {
            const [body] = args;
            this.request.addEventListener("load", () => {
              if (typeof this.onResponse !== "undefined") {
                const fetchResponse = createResponse(
                  this.request,
                  /**
                  * The `response` property is the right way to read
                  * the ambiguous response body, as the request's "responseType" may differ.
                  * @see https://xhr.spec.whatwg.org/#the-response-attribute
                  */
                  this.request.response
                );
                this.onResponse.call(this, {
                  response: fetchResponse,
                  isMockedResponse: this[kIsRequestHandled],
                  request: fetchRequest,
                  requestId: this.requestId
                });
              }
            });
            const requestBody = typeof body === "string" ? encodeBuffer(body) : body;
            const fetchRequest = this.toFetchApiRequest(requestBody);
            this[kFetchRequest] = fetchRequest.clone();
            queueMicrotask(() => {
              var _a20;
              (((_a20 = this.onRequest) == null ? void 0 : _a20.call(this, {
                request: fetchRequest,
                requestId: this.requestId
              })) || Promise.resolve()).finally(() => {
                if (!this[kIsRequestHandled]) {
                  this.logger.info("request callback settled but request has not been handled (readystate %d), performing as-is...", this.request.readyState);
                  if (IS_NODE2) this.request.setRequestHeader(INTERNAL_REQUEST_ID_HEADER_NAME, this.requestId);
                  return invoke();
                }
              });
            });
            break;
          }
          default:
            return invoke();
        }
      }
    });
    define(this.request, "upload", createProxy(this.request.upload, {
      setProperty: ([propertyName, nextValue], invoke) => {
        switch (propertyName) {
          case "onloadstart":
          case "onprogress":
          case "onaboart":
          case "onerror":
          case "onload":
          case "ontimeout":
          case "onloadend": {
            const eventName = propertyName.slice(2);
            this.registerUploadEvent(eventName, nextValue);
          }
        }
        return invoke();
      },
      methodCall: ([methodName, args], invoke) => {
        switch (methodName) {
          case "addEventListener": {
            const [eventName, listener] = args;
            this.registerUploadEvent(eventName, listener);
            this.logger.info("upload.addEventListener", eventName, listener);
            return invoke();
          }
        }
      }
    }));
  }
  registerEvent(eventName, listener) {
    const nextEvents = (this.events.get(eventName) || []).concat(listener);
    this.events.set(eventName, nextEvents);
    this.logger.info('registered event "%s"', eventName, listener);
  }
  registerUploadEvent(eventName, listener) {
    const nextEvents = (this.uploadEvents.get(eventName) || []).concat(listener);
    this.uploadEvents.set(eventName, nextEvents);
    this.logger.info('registered upload event "%s"', eventName, listener);
  }
  /**
  * Responds to the current request with the given
  * Fetch API `Response` instance.
  */
  async respondWith(response) {
    this[kIsRequestHandled] = true;
    if (this[kFetchRequest]) {
      const totalRequestBodyLength = await getBodyByteLength(this[kFetchRequest]);
      this.trigger("loadstart", this.request.upload, {
        loaded: 0,
        total: totalRequestBodyLength
      });
      this.trigger("progress", this.request.upload, {
        loaded: totalRequestBodyLength,
        total: totalRequestBodyLength
      });
      this.trigger("load", this.request.upload, {
        loaded: totalRequestBodyLength,
        total: totalRequestBodyLength
      });
      this.trigger("loadend", this.request.upload, {
        loaded: totalRequestBodyLength,
        total: totalRequestBodyLength
      });
    }
    this.logger.info("responding with a mocked response: %d %s", response.status, response.statusText);
    define(this.request, "status", response.status);
    define(this.request, "statusText", response.statusText);
    define(this.request, "responseURL", this.url.href);
    this.request.getResponseHeader = new Proxy(this.request.getResponseHeader, { apply: (_, __, args) => {
      this.logger.info("getResponseHeader", args[0]);
      if (this.request.readyState < this.request.HEADERS_RECEIVED) {
        this.logger.info("headers not received yet, returning null");
        return null;
      }
      const headerValue = response.headers.get(args[0]);
      this.logger.info('resolved response header "%s" to', args[0], headerValue);
      return headerValue;
    } });
    this.request.getAllResponseHeaders = new Proxy(this.request.getAllResponseHeaders, { apply: () => {
      this.logger.info("getAllResponseHeaders");
      if (this.request.readyState < this.request.HEADERS_RECEIVED) {
        this.logger.info("headers not received yet, returning empty string");
        return "";
      }
      const allHeaders = Array.from(response.headers.entries()).map(([headerName, headerValue]) => {
        return `${headerName}: ${headerValue}`;
      }).join("\r\n");
      this.logger.info("resolved all response headers to", allHeaders);
      return allHeaders;
    } });
    Object.defineProperties(this.request, {
      response: {
        enumerable: true,
        configurable: false,
        get: () => this.response
      },
      responseText: {
        enumerable: true,
        configurable: false,
        get: () => this.responseText
      },
      responseXML: {
        enumerable: true,
        configurable: false,
        get: () => this.responseXML
      }
    });
    const totalResponseBodyLength = await getBodyByteLength(response.clone());
    this.logger.info("calculated response body length", totalResponseBodyLength);
    this.trigger("loadstart", this.request, {
      loaded: 0,
      total: totalResponseBodyLength
    });
    this.setReadyState(this.request.HEADERS_RECEIVED);
    this.setReadyState(this.request.LOADING);
    const finalizeResponse = () => {
      this.logger.info("finalizing the mocked response...");
      this.setReadyState(this.request.DONE);
      this.trigger("load", this.request, {
        loaded: this.responseBuffer.byteLength,
        total: totalResponseBodyLength
      });
      this.trigger("loadend", this.request, {
        loaded: this.responseBuffer.byteLength,
        total: totalResponseBodyLength
      });
    };
    if (response.body) {
      this.logger.info("mocked response has body, streaming...");
      const reader = response.body.getReader();
      const readNextResponseBodyChunk = async () => {
        const { value, done } = await reader.read();
        if (done) {
          this.logger.info("response body stream done!");
          finalizeResponse();
          return;
        }
        if (value) {
          this.logger.info("read response body chunk:", value);
          this.responseBuffer = concatArrayBuffer(this.responseBuffer, value);
          this.trigger("progress", this.request, {
            loaded: this.responseBuffer.byteLength,
            total: totalResponseBodyLength
          });
        }
        readNextResponseBodyChunk();
      };
      readNextResponseBodyChunk();
    } else finalizeResponse();
  }
  responseBufferToText() {
    return decodeBuffer(this.responseBuffer);
  }
  get response() {
    this.logger.info("getResponse (responseType: %s)", this.request.responseType);
    if (this.request.readyState !== this.request.DONE) return null;
    switch (this.request.responseType) {
      case "json": {
        const responseJson = parseJson(this.responseBufferToText());
        this.logger.info("resolved response JSON", responseJson);
        return responseJson;
      }
      case "arraybuffer": {
        const arrayBuffer = toArrayBuffer(this.responseBuffer);
        this.logger.info("resolved response ArrayBuffer", arrayBuffer);
        return arrayBuffer;
      }
      case "blob": {
        const mimeType = this.request.getResponseHeader("Content-Type") || "text/plain";
        const responseBlob = new Blob([this.responseBufferToText()], { type: mimeType });
        this.logger.info("resolved response Blob (mime type: %s)", responseBlob, mimeType);
        return responseBlob;
      }
      default: {
        const responseText = this.responseBufferToText();
        this.logger.info('resolving "%s" response type as text', this.request.responseType, responseText);
        return responseText;
      }
    }
  }
  get responseText() {
    invariant2(this.request.responseType === "" || this.request.responseType === "text", "InvalidStateError: The object is in invalid state.");
    if (this.request.readyState !== this.request.LOADING && this.request.readyState !== this.request.DONE) return "";
    const responseText = this.responseBufferToText();
    this.logger.info('getResponseText: "%s"', responseText);
    return responseText;
  }
  get responseXML() {
    invariant2(this.request.responseType === "" || this.request.responseType === "document", "InvalidStateError: The object is in invalid state.");
    if (this.request.readyState !== this.request.DONE) return null;
    const contentType = this.request.getResponseHeader("Content-Type") || "";
    if (typeof DOMParser === "undefined") {
      console.warn("Cannot retrieve XMLHttpRequest response body as XML: DOMParser is not defined. You are likely using an environment that is not browser or does not polyfill browser globals correctly.");
      return null;
    }
    if (isDomParserSupportedType(contentType)) return new DOMParser().parseFromString(this.responseBufferToText(), contentType);
    return null;
  }
  errorWith(error2) {
    this[kIsRequestHandled] = true;
    this.logger.info("responding with an error");
    this.setReadyState(this.request.DONE);
    this.trigger("error", this.request);
    this.trigger("loadend", this.request);
  }
  /**
  * Transitions this request's `readyState` to the given one.
  */
  setReadyState(nextReadyState) {
    this.logger.info("setReadyState: %d -> %d", this.request.readyState, nextReadyState);
    if (this.request.readyState === nextReadyState) {
      this.logger.info("ready state identical, skipping transition...");
      return;
    }
    define(this.request, "readyState", nextReadyState);
    this.logger.info("set readyState to: %d", nextReadyState);
    if (nextReadyState !== this.request.UNSENT) {
      this.logger.info('triggering "readystatechange" event...');
      this.trigger("readystatechange", this.request);
    }
  }
  /**
  * Triggers given event on the `XMLHttpRequest` instance.
  */
  trigger(eventName, target, options) {
    const callback = target[`on${eventName}`];
    const event = createEvent(target, eventName, options);
    this.logger.info('trigger "%s"', eventName, options || "");
    if (typeof callback === "function") {
      this.logger.info('found a direct "%s" callback, calling...', eventName);
      callback.call(target, event);
    }
    const events = target instanceof XMLHttpRequestUpload ? this.uploadEvents : this.events;
    for (const [registeredEventName, listeners] of events) if (registeredEventName === eventName) {
      this.logger.info('found %d listener(s) for "%s" event, calling...', listeners.length, eventName);
      listeners.forEach((listener) => listener.call(target, event));
    }
  }
  /**
  * Converts this `XMLHttpRequest` instance into a Fetch API `Request` instance.
  */
  toFetchApiRequest(body) {
    this.logger.info("converting request to a Fetch API Request...");
    const resolvedBody = body instanceof Document ? body.documentElement.innerText : body;
    const fetchRequest = new Request(this.url.href, {
      method: this.method,
      headers: this.requestHeaders,
      credentials: this.request.withCredentials ? "include" : "same-origin",
      body: ["GET", "HEAD"].includes(this.method.toUpperCase()) ? null : resolvedBody
    });
    define(fetchRequest, "headers", createProxy(fetchRequest.headers, { methodCall: ([methodName, args], invoke) => {
      switch (methodName) {
        case "append":
        case "set": {
          const [headerName, headerValue] = args;
          this.request.setRequestHeader(headerName, headerValue);
          break;
        }
        case "delete": {
          const [headerName] = args;
          console.warn(`XMLHttpRequest: Cannot remove a "${headerName}" header from the Fetch API representation of the "${fetchRequest.method} ${fetchRequest.url}" request. XMLHttpRequest headers cannot be removed.`);
          break;
        }
      }
      return invoke();
    } }));
    setRawRequest(fetchRequest, this.request);
    this.logger.info("converted request to a Fetch API Request!", fetchRequest);
    return fetchRequest;
  }
};
function toAbsoluteUrl(url) {
  if (typeof location === "undefined") return new URL(url);
  return new URL(url.toString(), location.href);
}
function define(target, property, value) {
  Reflect.defineProperty(target, property, {
    writable: true,
    enumerable: true,
    value
  });
}
function createXMLHttpRequestProxy({ emitter, logger }) {
  return new Proxy(globalThis.XMLHttpRequest, { construct(target, args, newTarget) {
    logger.info("constructed new XMLHttpRequest");
    const originalRequest = Reflect.construct(target, args, newTarget);
    const prototypeDescriptors = Object.getOwnPropertyDescriptors(target.prototype);
    for (const propertyName in prototypeDescriptors) Reflect.defineProperty(originalRequest, propertyName, prototypeDescriptors[propertyName]);
    const xhrRequestController = new XMLHttpRequestController(originalRequest, logger);
    xhrRequestController.onRequest = async function({ request, requestId }) {
      const controller = new RequestController(request, {
        passthrough: () => {
          this.logger.info("no mocked response received, performing request as-is...");
        },
        respondWith: async (response) => {
          if (isResponseError(response)) {
            this.errorWith(new TypeError("Network error"));
            return;
          }
          await this.respondWith(response);
        },
        errorWith: (reason) => {
          this.logger.info("request errored!", { error: reason });
          if (reason instanceof Error) this.errorWith(reason);
        }
      });
      this.logger.info("awaiting mocked response...");
      this.logger.info('emitting the "request" event for %s listener(s)...', emitter.listenerCount("request"));
      await handleRequest({
        request,
        requestId,
        controller,
        emitter
      });
    };
    xhrRequestController.onResponse = async function({ response, isMockedResponse, request, requestId }) {
      this.logger.info('emitting the "response" event for %s listener(s)...', emitter.listenerCount("response"));
      emitter.emit("response", {
        response,
        isMockedResponse,
        request,
        requestId
      });
    };
    return xhrRequestController.request;
  } });
}
var _a18;
var XMLHttpRequestInterceptor = (_a18 = class extends Interceptor2 {
  constructor() {
    super(_a18.interceptorSymbol);
  }
  checkEnvironment() {
    return hasConfigurableGlobal2("XMLHttpRequest");
  }
  setup() {
    const logger = this.logger.extend("setup");
    logger.info('patching "XMLHttpRequest" module...');
    const PureXMLHttpRequest = globalThis.XMLHttpRequest;
    invariant2(!PureXMLHttpRequest[IS_PATCHED_MODULE], 'Failed to patch the "XMLHttpRequest" module: already patched.');
    globalThis.XMLHttpRequest = createXMLHttpRequestProxy({
      emitter: this.emitter,
      logger: this.logger
    });
    logger.info('native "XMLHttpRequest" module patched!', globalThis.XMLHttpRequest.name);
    Object.defineProperty(globalThis.XMLHttpRequest, IS_PATCHED_MODULE, {
      enumerable: true,
      configurable: true,
      value: true
    });
    this.subscriptions.push(() => {
      Object.defineProperty(globalThis.XMLHttpRequest, IS_PATCHED_MODULE, { value: void 0 });
      globalThis.XMLHttpRequest = PureXMLHttpRequest;
      logger.info('native "XMLHttpRequest" module restored!', globalThis.XMLHttpRequest.name);
    });
  }
}, _a18.interceptorSymbol = Symbol("xhr"), _a18);
var _FallbackHttpSource_instances, printStartMessage_fn2, printStopMessage_fn2, _a19;
var FallbackHttpSource = (_a19 = class extends InterceptorSource {
  constructor(options) {
    super({
      interceptors: [new XMLHttpRequestInterceptor(), new FetchInterceptor()]
    });
    __privateAdd(this, _FallbackHttpSource_instances);
    this.options = options;
  }
  enable() {
    super.enable();
    if (!this.options.quiet) {
      __privateMethod(this, _FallbackHttpSource_instances, printStartMessage_fn2).call(this);
    }
  }
  disable() {
    super.disable();
    if (!this.options.quiet) {
      __privateMethod(this, _FallbackHttpSource_instances, printStopMessage_fn2).call(this);
    }
  }
}, _FallbackHttpSource_instances = new WeakSet(), printStartMessage_fn2 = function() {
  console.groupCollapsed(
    `%c${devUtils.formatMessage("Mocking enabled (fallback mode).")}`,
    "color:orangered;font-weight:bold;"
  );
  console.log(
    "%cDocumentation: %chttps://mswjs.io/docs",
    "font-weight:bold",
    "font-weight:normal"
  );
  console.log("Found an issue? https://github.com/mswjs/msw/issues");
  console.groupEnd();
}, printStopMessage_fn2 = function() {
  console.log(
    `%c${devUtils.formatMessage("Mocking disabled.")}`,
    "color:orangered;font-weight:bold;"
  );
}, _a19);
var DEFAULT_WORKER_URL = "/mockServiceWorker.js";
function setupWorker(...handlers) {
  invariant2(
    !isNodeProcess(),
    devUtils.formatMessage(
      "Failed to execute `setupWorker` in a non-browser environment"
    )
  );
  const network = defineNetwork({
    sources: [],
    handlers
  });
  return {
    async start(options) {
      var _a20, _b2, _c2;
      if ((options == null ? void 0 : options.waitUntilReady) != null) {
        devUtils.warn(
          `The "waitUntilReady" option has been deprecated. Please remove it from this "worker.start()" call. Follow the recommended Browser integration (https://mswjs.io/docs/integrations/browser) to eliminate any race conditions between the Service Worker registration and any requests made by your application on initial render.`
        );
      }
      if (network.readyState === NetworkReadyState.ENABLED) {
        devUtils.warn(
          'Found a redundant "worker.start()" call. Note that starting the worker while mocking is already enabled will have no effect. Consider removing this "worker.start()" call.'
        );
        return;
      }
      const httpSource = supportsServiceWorker() ? await ServiceWorkerSource.from({
        serviceWorker: {
          url: ((_b2 = (_a20 = options == null ? void 0 : options.serviceWorker) == null ? void 0 : _a20.url) == null ? void 0 : _b2.toString()) || DEFAULT_WORKER_URL,
          options: (_c2 = options == null ? void 0 : options.serviceWorker) == null ? void 0 : _c2.options
        },
        findWorker: options == null ? void 0 : options.findWorker,
        quiet: options == null ? void 0 : options.quiet
      }) : new FallbackHttpSource({
        quiet: options == null ? void 0 : options.quiet
      });
      network.configure({
        sources: [
          httpSource,
          new InterceptorSource({
            interceptors: [new WebSocketInterceptor2()]
          })
        ],
        onUnhandledFrame: fromLegacyOnUnhandledRequest(() => {
          return (options == null ? void 0 : options.onUnhandledRequest) || "warn";
        }),
        context: {
          quiet: options == null ? void 0 : options.quiet
        }
      });
      await network.enable();
      if (httpSource instanceof ServiceWorkerSource) {
        const [, registration] = await httpSource.workerPromise;
        return registration;
      }
    },
    stop() {
      if (network.readyState === NetworkReadyState.DISABLED) {
        devUtils.warn(
          `Found a redundant "worker.stop()" call. Notice that stopping the worker after it has already been stopped has no effect. Consider removing this "worker.stop()" call.`
        );
        return;
      }
      network.disable();
      window.postMessage({ type: "msw/worker:stop" });
    },
    events: network.events,
    use: network.use.bind(network),
    resetHandlers: network.resetHandlers.bind(network),
    restoreHandlers: network.restoreHandlers.bind(network),
    listHandlers: network.listHandlers.bind(network)
  };
}
var SetupWorkerApi = class {
  constructor() {
    __publicField(this, "start");
    __publicField(this, "stop");
    __publicField(this, "use");
    __publicField(this, "resetHandlers");
    __publicField(this, "restoreHandlers");
    __publicField(this, "listHandlers");
    __publicField(this, "events");
    const worker = setupWorker();
    this.start = worker.start.bind(worker);
    this.stop = worker.stop.bind(worker);
    this.use = worker.use.bind(worker);
    this.resetHandlers = worker.resetHandlers.bind(worker);
    this.restoreHandlers = worker.restoreHandlers.bind(worker);
    this.listHandlers = worker.listHandlers.bind(worker);
    this.events = worker.events;
  }
};
export {
  SetupWorkerApi,
  setupWorker
};
//# sourceMappingURL=msw_browser.js.map
